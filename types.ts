import { Index as AchievementIndex, Achievement } from "./types/Achievement";
import { Index as AchievementCategoryIndex, AchievementCategory } from "./types/AchievementCategory";
import { Index as AchievementHideConditionIndex, AchievementHideCondition } from "./types/AchievementHideCondition";
import { Index as AchievementKindIndex, AchievementKind } from "./types/AchievementKind";
import { Index as ActionIndex, Action } from "./types/Action";
import { Index as ActionCastTimelineIndex, ActionCastTimeline } from "./types/ActionCastTimeline";
import { Index as ActionCastVFXIndex, ActionCastVFX } from "./types/ActionCastVFX";
import { Index as ActionCategoryIndex, ActionCategory } from "./types/ActionCategory";
import { Index as ActionComboRouteIndex, ActionComboRoute } from "./types/ActionComboRoute";
import { Index as ActionIndirectionIndex, ActionIndirection } from "./types/ActionIndirection";
import { Index as ActionParamIndex, ActionParam } from "./types/ActionParam";
import { Index as ActionProcStatusIndex, ActionProcStatus } from "./types/ActionProcStatus";
import { Index as ActionTimelineIndex, ActionTimeline } from "./types/ActionTimeline";
import { Index as ActionTimelineMoveIndex, ActionTimelineMove } from "./types/ActionTimelineMove";
import { Index as ActionTimelineReplaceIndex, ActionTimelineReplace } from "./types/ActionTimelineReplace";
import { Index as ActionTransientIndex, ActionTransient } from "./types/ActionTransient";
import { Index as ActivityFeedButtonsIndex, ActivityFeedButtons } from "./types/ActivityFeedButtons";
import { Index as ActivityFeedCaptionsIndex, ActivityFeedCaptions } from "./types/ActivityFeedCaptions";
import { Index as ActivityFeedGroupCaptionsIndex, ActivityFeedGroupCaptions } from "./types/ActivityFeedGroupCaptions";
import { Index as ActivityFeedImagesIndex, ActivityFeedImages } from "./types/ActivityFeedImages";
import { Index as AddonIndex, Addon } from "./types/Addon";
import { Index as AddonHudIndex, AddonHud } from "./types/AddonHud";
import { Index as AdventureIndex, Adventure } from "./types/Adventure";
import { Index as AdventureExPhaseIndex, AdventureExPhase } from "./types/AdventureExPhase";
import { Index as AetherCurrentIndex, AetherCurrent } from "./types/AetherCurrent";
import { Index as AetherCurrentCompFlgSetIndex, AetherCurrentCompFlgSet } from "./types/AetherCurrentCompFlgSet";
import { Index as AetherialWheelIndex, AetherialWheel } from "./types/AetherialWheel";
import { Index as AetheryteIndex, Aetheryte } from "./types/Aetheryte";
import { Index as AetheryteSystemDefineIndex, AetheryteSystemDefine } from "./types/AetheryteSystemDefine";
import { Index as AirshipExplorationLevelIndex, AirshipExplorationLevel } from "./types/AirshipExplorationLevel";
import { Index as AirshipExplorationLogIndex, AirshipExplorationLog } from "./types/AirshipExplorationLog";
import { Index as AirshipExplorationParamTypeIndex, AirshipExplorationParamType } from "./types/AirshipExplorationParamType";
import { Index as AirshipExplorationPartIndex, AirshipExplorationPart } from "./types/AirshipExplorationPart";
import { Index as AirshipExplorationPointIndex, AirshipExplorationPoint } from "./types/AirshipExplorationPoint";
import { Index as AnimationLODIndex, AnimationLOD } from "./types/AnimationLOD";
import { Index as AnimaWeapon5Index, AnimaWeapon5 } from "./types/AnimaWeapon5";
import { Index as AnimaWeapon5ParamIndex, AnimaWeapon5Param } from "./types/AnimaWeapon5Param";
import { Index as AnimaWeapon5PatternGroupIndex, AnimaWeapon5PatternGroup } from "./types/AnimaWeapon5PatternGroup";
import { Index as AnimaWeapon5SpiritTalkIndex, AnimaWeapon5SpiritTalk } from "./types/AnimaWeapon5SpiritTalk";
import { Index as AnimaWeapon5SpiritTalkParamIndex, AnimaWeapon5SpiritTalkParam } from "./types/AnimaWeapon5SpiritTalkParam";
import { Index as AnimaWeapon5TradeItemIndex, AnimaWeapon5TradeItem } from "./types/AnimaWeapon5TradeItem";
import { Index as AnimaWeaponFUITalkIndex, AnimaWeaponFUITalk } from "./types/AnimaWeaponFUITalk";
import { Index as AnimaWeaponFUITalkParamIndex, AnimaWeaponFUITalkParam } from "./types/AnimaWeaponFUITalkParam";
import { Index as AnimaWeaponIconIndex, AnimaWeaponIcon } from "./types/AnimaWeaponIcon";
import { Index as AnimaWeaponItemIndex } from "./types/AnimaWeaponItem";
import { Index as AozActionIndex, AozAction } from "./types/AozAction";
import { Index as AozActionTransientIndex, AozActionTransient } from "./types/AozActionTransient";
import { Index as AOZArrangementIndex, AOZArrangement } from "./types/AOZArrangement";
import { Index as AOZBossIndex, AOZBoss } from "./types/AOZBoss";
import { Index as AOZContentIndex, AOZContent } from "./types/AOZContent";
import { Index as AOZContentBriefingBNpcIndex, AOZContentBriefingBNpc } from "./types/AOZContentBriefingBNpc";
import { Index as AquariumFishIndex, AquariumFish } from "./types/AquariumFish";
import { Index as AquariumWaterIndex, AquariumWater } from "./types/AquariumWater";
import { Index as ArrayEventHandlerIndex, ArrayEventHandler } from "./types/ArrayEventHandler";
import { Index as AttackTypeIndex, AttackType } from "./types/AttackType";
import { Index as BacklightColorIndex, BacklightColor } from "./types/BacklightColor";
import { Index as BallistaIndex, Ballista } from "./types/Ballista";
import { Index as BalloonIndex, Balloon } from "./types/Balloon";
import { Index as BaseParamIndex, BaseParam } from "./types/BaseParam";
import { Index as BattleLeveIndex, BattleLeve } from "./types/BattleLeve";
import { Index as BattleLeveRuleIndex, BattleLeveRule } from "./types/BattleLeveRule";
import { Index as BeastRankBonusIndex, BeastRankBonus } from "./types/BeastRankBonus";
import { Index as BeastReputationRankIndex, BeastReputationRank } from "./types/BeastReputationRank";
import { Index as BeastTribeIndex, BeastTribe } from "./types/BeastTribe";
import { Index as BehaviorIndex, Behavior } from "./types/Behavior";
import { Index as BehaviorPathIndex, BehaviorPath } from "./types/BehaviorPath";
import { Index as BenchmarkOverrideEquipmentIndex, BenchmarkOverrideEquipment } from "./types/BenchmarkOverrideEquipment";
import { Index as BGMIndex, BGM } from "./types/BGM";
import { Index as BGMFadeIndex, BGMFade } from "./types/BGMFade";
import { Index as BGMFadeTypeIndex, BGMFadeType } from "./types/BGMFadeType";
import { Index as BGMSceneIndex, BGMScene } from "./types/BGMScene";
import { Index as BGMSituationIndex, BGMSituation } from "./types/BGMSituation";
import { Index as BGMSwitchIndex, BGMSwitch } from "./types/BGMSwitch";
import { Index as BGMSystemDefineIndex, BGMSystemDefine } from "./types/BGMSystemDefine";
import { Index as BNpcAnnounceIconIndex, BNpcAnnounceIcon } from "./types/BNpcAnnounceIcon";
import { Index as BNpcBaseIndex, BNpcBase } from "./types/BNpcBase";
import { Index as BNpcCustomizeIndex, BNpcCustomize } from "./types/BNpcCustomize";
import { Index as BNpcNameIndex, BNpcName } from "./types/BNpcName";
import { Index as BNpcPartsIndex, BNpcParts } from "./types/BNpcParts";
import { Index as BNpcStateIndex, BNpcState } from "./types/BNpcState";
import { Index as BuddyIndex, Buddy } from "./types/Buddy";
import { Index as BuddyActionIndex, BuddyAction } from "./types/BuddyAction";
import { Index as BuddyEquipIndex, BuddyEquip } from "./types/BuddyEquip";
import { Index as BuddyItemIndex, BuddyItem } from "./types/BuddyItem";
import { Index as BuddyRankIndex, BuddyRank } from "./types/BuddyRank";
import { Index as BuddySkillIndex, BuddySkill } from "./types/BuddySkill";
import { Index as CabinetIndex, Cabinet } from "./types/Cabinet";
import { Index as CabinetCategoryIndex, CabinetCategory } from "./types/CabinetCategory";
import { Index as CalendarIndex, Calendar } from "./types/Calendar";
import { Index as CarryIndex, Carry } from "./types/Carry";
import { Index as ChannelingIndex, Channeling } from "./types/Channeling";
import { Index as CharaMakeClassEquipIndex, CharaMakeClassEquip } from "./types/CharaMakeClassEquip";
import { Index as CharaMakeCustomizeIndex, CharaMakeCustomize } from "./types/CharaMakeCustomize";
import { Index as CharaMakeNameIndex, CharaMakeName } from "./types/CharaMakeName";
import { Index as CharaMakeTypeIndex, CharaMakeType } from "./types/CharaMakeType";
import { Index as ChocoboRaceIndex, ChocoboRace } from "./types/ChocoboRace";
import { Index as ChocoboRaceAbilityIndex, ChocoboRaceAbility } from "./types/ChocoboRaceAbility";
import { Index as ChocoboRaceAbilityTypeIndex, ChocoboRaceAbilityType } from "./types/ChocoboRaceAbilityType";
import { Index as ChocoboRaceItemIndex, ChocoboRaceItem } from "./types/ChocoboRaceItem";
import { Index as ChocoboRaceRankIndex, ChocoboRaceRank } from "./types/ChocoboRaceRank";
import { Index as ChocoboRaceStatusIndex, ChocoboRaceStatus } from "./types/ChocoboRaceStatus";
import { Index as ChocoboRaceTerritoryIndex, ChocoboRaceTerritory } from "./types/ChocoboRaceTerritory";
import { Index as ChocoboRaceTutorialIndex, ChocoboRaceTutorial } from "./types/ChocoboRaceTutorial";
import { Index as ChocoboRaceWeatherIndex, ChocoboRaceWeather } from "./types/ChocoboRaceWeather";
import { Index as ChocoboTaxiIndex, ChocoboTaxi } from "./types/ChocoboTaxi";
import { Index as ChocoboTaxiStandIndex, ChocoboTaxiStand } from "./types/ChocoboTaxiStand";
import { Index as CircleActivityIndex, CircleActivity } from "./types/CircleActivity";
import { Index as ClassJobIndex, ClassJob } from "./types/ClassJob";
import { Index as ClassJobCategoryIndex, ClassJobCategory } from "./types/ClassJobCategory";
import { Index as CollectablesShopIndex, CollectablesShop } from "./types/CollectablesShop";
import { Index as CollectablesShopItemIndex, CollectablesShopItem } from "./types/CollectablesShopItem";
import { Index as CollectablesShopItemGroupIndex, CollectablesShopItemGroup } from "./types/CollectablesShopItemGroup";
import { Index as CollectablesShopRefineIndex, CollectablesShopRefine } from "./types/CollectablesShopRefine";
import { Index as CollectablesShopRewardItemIndex, CollectablesShopRewardItem } from "./types/CollectablesShopRewardItem";
import { Index as CollectablesShopRewardScripIndex, CollectablesShopRewardScrip } from "./types/CollectablesShopRewardScrip";
import { Index as CompanionIndex, Companion } from "./types/Companion";
import { Index as CompanionMoveIndex, CompanionMove } from "./types/CompanionMove";
import { Index as CompanionTransientIndex, CompanionTransient } from "./types/CompanionTransient";
import { Index as CompanyActionIndex, CompanyAction } from "./types/CompanyAction";
import { Index as CompanyCraftDraftIndex, CompanyCraftDraft } from "./types/CompanyCraftDraft";
import { Index as CompanyCraftDraftCategoryIndex, CompanyCraftDraftCategory } from "./types/CompanyCraftDraftCategory";
import { Index as CompanyCraftManufactoryStateIndex, CompanyCraftManufactoryState } from "./types/CompanyCraftManufactoryState";
import { Index as CompanyCraftPartIndex, CompanyCraftPart } from "./types/CompanyCraftPart";
import { Index as CompanyCraftProcessIndex, CompanyCraftProcess } from "./types/CompanyCraftProcess";
import { Index as CompanyCraftSequenceIndex, CompanyCraftSequence } from "./types/CompanyCraftSequence";
import { Index as CompanyCraftSupplyItemIndex, CompanyCraftSupplyItem } from "./types/CompanyCraftSupplyItem";
import { Index as CompanyCraftTypeIndex, CompanyCraftType } from "./types/CompanyCraftType";
import { Index as CompanyLeveIndex, CompanyLeve } from "./types/CompanyLeve";
import { Index as CompanyLeveRuleIndex, CompanyLeveRule } from "./types/CompanyLeveRule";
import { Index as CompleteJournalIndex, CompleteJournal } from "./types/CompleteJournal";
import { Index as CompleteJournalCategoryIndex, CompleteJournalCategory } from "./types/CompleteJournalCategory";
import { Index as ConditionIndex, Condition } from "./types/Condition";
import { Index as ConfigKeyIndex, ConfigKey } from "./types/ConfigKey";
import { Index as ContentCloseCycleIndex, ContentCloseCycle } from "./types/ContentCloseCycle";
import { Index as ContentExActionIndex, ContentExAction } from "./types/ContentExAction";
import { Index as ContentFinderConditionIndex, ContentFinderCondition } from "./types/ContentFinderCondition";
import { Index as ContentFinderConditionTransientIndex, ContentFinderConditionTransient } from "./types/ContentFinderConditionTransient";
import { Index as ContentGaugeIndex, ContentGauge } from "./types/ContentGauge";
import { Index as ContentGaugeColorIndex, ContentGaugeColor } from "./types/ContentGaugeColor";
import { Index as ContentMemberTypeIndex, ContentMemberType } from "./types/ContentMemberType";
import { Index as ContentNpcTalkIndex, ContentNpcTalk } from "./types/ContentNpcTalk";
import { Index as ContentRandomSelectIndex, ContentRandomSelect } from "./types/ContentRandomSelect";
import { Index as ContentRouletteIndex, ContentRoulette } from "./types/ContentRoulette";
import { Index as ContentRouletteOpenRuleIndex, ContentRouletteOpenRule } from "./types/ContentRouletteOpenRule";
import { Index as ContentRouletteRoleBonusIndex, ContentRouletteRoleBonus } from "./types/ContentRouletteRoleBonus";
import { Index as ContentsNoteIndex, ContentsNote } from "./types/ContentsNote";
import { Index as ContentTalkIndex, ContentTalk } from "./types/ContentTalk";
import { Index as ContentTalkParamIndex, ContentTalkParam } from "./types/ContentTalkParam";
import { Index as ContentTypeIndex, ContentType } from "./types/ContentType";
import { Index as CraftActionIndex, CraftAction } from "./types/CraftAction";
import { Index as CraftLeveIndex, CraftLeve } from "./types/CraftLeve";
import { Index as CraftLevelDifferenceIndex, CraftLevelDifference } from "./types/CraftLevelDifference";
import { Index as CraftTypeIndex, CraftType } from "./types/CraftType";
import { Index as CreditIndex, Credit } from "./types/Credit";
import { Index as CreditBackImageIndex } from "./types/CreditBackImage";
import { Index as CreditCastIndex, CreditCast } from "./types/CreditCast";
import { Index as CreditListIndex  } from "./types/CreditList";
import { Index as CreditListTextIndex, CreditListText } from "./types/CreditListText";
import { Index as CurrencyIndex, Currency } from "./types/Currency";
import { Index as CustomTalkIndex, CustomTalk } from "./types/CustomTalk";
import { Index as CustomTalkDynamicIconIndex, CustomTalkDynamicIcon } from "./types/CustomTalkDynamicIcon";
import { Index as CustomTalkNestHandlersIndex, CustomTalkNestHandlers } from "./types/CustomTalkNestHandlers";
import { Index as CutsceneIndex, Cutscene } from "./types/Cutscene";
import { Index as CutsceneMotionIndex, CutsceneMotion } from "./types/CutsceneMotion";
import { Index as CutsceneWorkIndexIndex, CutsceneWorkIndex } from "./types/CutsceneWorkIndex";
import { Index as CutScreenImageIndex, CutScreenImage } from "./types/CutScreenImage";
import { Index as CycleTimeIndex, CycleTime } from "./types/CycleTime";
import { Index as DailySupplyItemIndex, DailySupplyItem } from "./types/DailySupplyItem";
import { Index as DawnContentIndex, DawnContent } from "./types/DawnContent";
import { Index as DawnGrowMemberIndex, DawnGrowMember } from "./types/DawnGrowMember";
import { Index as DawnMemberUIParamIndex, DawnMemberUIParam } from "./types/DawnMemberUIParam";
import { Index as DawnQuestAnnounceIndex, DawnQuestAnnounce } from "./types/DawnQuestAnnounce";
import { Index as DawnQuestMemberIndex, DawnQuestMember } from "./types/DawnQuestMember";
import { Index as DeepDungeonIndex, DeepDungeon } from "./types/DeepDungeon";
import { Index as DeepDungeonBanIndex, DeepDungeonBan } from "./types/DeepDungeonBan";
import { Index as DeepDungeonDangerIndex, DeepDungeonDanger } from "./types/DeepDungeonDanger";
import { Index as DeepDungeonEquipmentIndex, DeepDungeonEquipment } from "./types/DeepDungeonEquipment";
import { Index as DeepDungeonFloorEffectUIIndex, DeepDungeonFloorEffectUI } from "./types/DeepDungeonFloorEffectUI";
import { Index as DeepDungeonItemIndex, DeepDungeonItem } from "./types/DeepDungeonItem";
import { Index as DeepDungeonLayerIndex, DeepDungeonLayer } from "./types/DeepDungeonLayer";
import { Index as DeepDungeonMagicStoneIndex, DeepDungeonMagicStone } from "./types/DeepDungeonMagicStone";
import { Index as DeepDungeonMap5XIndex, DeepDungeonMap5X } from "./types/DeepDungeonMap5X";
import { Index as DeepDungeonRoomIndex, DeepDungeonRoom } from "./types/DeepDungeonRoom";
import { Index as DeepDungeonStatusIndex, DeepDungeonStatus } from "./types/DeepDungeonStatus";
import { Index as DefaultTalkIndex, DefaultTalk } from "./types/DefaultTalk";
import { Index as DefaultTalkLipSyncTypeIndex, DefaultTalkLipSyncType } from "./types/DefaultTalkLipSyncType";
import { Index as DeliveryQuestIndex, DeliveryQuest } from "./types/DeliveryQuest";
import { Index as DescriptionIndex, Description } from "./types/Description";
import { Index as DescriptionPageIndex, DescriptionPage } from "./types/DescriptionPage";
import { Index as DescriptionSectionIndex, DescriptionSection } from "./types/DescriptionSection";
import { Index as DescriptionStringIndex, DescriptionString } from "./types/DescriptionString";
import { Index as DisposalShopIndex, DisposalShop } from "./types/DisposalShop";
import { Index as DisposalShopFilterTypeIndex, DisposalShopFilterType } from "./types/DisposalShopFilterType";
import { Index as DisposalShopItemIndex, DisposalShopItem } from "./types/DisposalShopItem";
import { Index as DpsChallengeIndex, DpsChallenge } from "./types/DpsChallenge";
import { Index as DpsChallengeOfficerIndex, DpsChallengeOfficer } from "./types/DpsChallengeOfficer";
import { Index as DpsChallengeTransientIndex, DpsChallengeTransient } from "./types/DpsChallengeTransient";
import { Index as EmjAddonIndex, EmjAddon } from "./types/EmjAddon";
import { Index as EmjDaniIndex, EmjDani } from "./types/EmjDani";
import { Index as EmoteIndex, Emote } from "./types/Emote";
import { Index as EmoteCategoryIndex, EmoteCategory } from "./types/EmoteCategory";
import { Index as EmoteModeIndex, EmoteMode } from "./types/EmoteMode";
import { Index as ENpcBaseIndex, ENpcBase } from "./types/ENpcBase";
import { Index as ENpcDressUpIndex, ENpcDressUp } from "./types/ENpcDressUp";
import { Index as ENpcDressUpDressIndex, ENpcDressUpDress } from "./types/ENpcDressUpDress";
import { Index as ENpcResidentIndex, ENpcResident } from "./types/ENpcResident";
import { Index as EObjIndex, EObj } from "./types/EObj";
import { Index as EObjNameIndex, EObjName } from "./types/EObjName";
import { Index as EquipRaceCategoryIndex, EquipRaceCategory } from "./types/EquipRaceCategory";
import { Index as EquipSlotCategoryIndex, EquipSlotCategory } from "./types/EquipSlotCategory";
import { Index as EurekaAetherItemIndex, EurekaAetherItem } from "./types/EurekaAetherItem";
import { Index as EurekaAethernetIndex, EurekaAethernet } from "./types/EurekaAethernet";
import { Index as EurekaGrowDataIndex, EurekaGrowData } from "./types/EurekaGrowData";
import { Index as EurekaLogosMixerProbabilityIndex, EurekaLogosMixerProbability } from "./types/EurekaLogosMixerProbability";
import { Index as EurekaMagiaActionIndex, EurekaMagiaAction } from "./types/EurekaMagiaAction";
import { Index as EurekaMagiciteItemIndex, EurekaMagiciteItem } from "./types/EurekaMagiciteItem";
import { Index as EurekaMagiciteItemTypeIndex, EurekaMagiciteItemType } from "./types/EurekaMagiciteItemType";
import { Index as EurekaSphereElementAdjustIndex, EurekaSphereElementAdjust } from "./types/EurekaSphereElementAdjust";
import { Index as EventActionIndex, EventAction } from "./types/EventAction";
import { Index as EventIconPriorityIndex, EventIconPriority } from "./types/EventIconPriority";
import { Index as EventIconTypeIndex, EventIconType } from "./types/EventIconType";
import { Index as EventItemIndex, EventItem } from "./types/EventItem";
import { Index as EventItemCastTimelineIndex, EventItemCastTimeline } from "./types/EventItemCastTimeline";
import { Index as EventItemHelpIndex, EventItemHelp } from "./types/EventItemHelp";
import { Index as EventItemTimelineIndex, EventItemTimeline } from "./types/EventItemTimeline";
import { Index as EventSystemDefineIndex, EventSystemDefine } from "./types/EventSystemDefine";
import { Index as ExportedSGIndex, ExportedSG } from "./types/ExportedSG";
import { Index as ExVersionIndex, ExVersion } from "./types/ExVersion";
import { Index as FateIndex, Fate } from "./types/Fate";
import { Index as FateEventIndex, FateEvent } from "./types/FateEvent";
import { Index as FateModeIndex, FateMode } from "./types/FateMode";
import { Index as FateProgressUIIndex, FateProgressUI } from "./types/FateProgressUI";
import { Index as FateTokenTypeIndex, FateTokenType } from "./types/FateTokenType";
import { Index as FCActivityIndex, FCActivity } from "./types/FCActivity";
import { Index as FCActivityCategoryIndex, FCActivityCategory } from "./types/FCActivityCategory";
import { Index as FCAuthorityIndex, FCAuthority } from "./types/FCAuthority";
import { Index as FCAuthorityCategoryIndex, FCAuthorityCategory } from "./types/FCAuthorityCategory";
import { Index as FCChestNameIndex, FCChestName } from "./types/FCChestName";
import { Index as FCCrestSymbolIndex, FCCrestSymbol } from "./types/FCCrestSymbol";
import { Index as FccShopIndex, FccShop } from "./types/FccShop";
import { Index as FCHierarchyIndex, FCHierarchy } from "./types/FCHierarchy";
import { Index as FCProfileIndex, FCProfile } from "./types/FCProfile";
import { Index as FCRankIndex, FCRank } from "./types/FCRank";
import { Index as FCReputationIndex, FCReputation } from "./types/FCReputation";
import { Index as FCRightsIndex, FCRights } from "./types/FCRights";
import { Index as FestivalIndex, Festival } from "./types/Festival";
import { Index as FieldMarkerIndex, FieldMarker } from "./types/FieldMarker";
import { Index as FishingRecordTypeIndex, FishingRecordType } from "./types/FishingRecordType";
import { Index as FishingRecordTypeTransientIndex, FishingRecordTypeTransient } from "./types/FishingRecordTypeTransient";
import { Index as FishingSpotIndex, FishingSpot } from "./types/FishingSpot";
import { Index as FishParameterIndex, FishParameter } from "./types/FishParameter";
import { Index as Frontline03Index, Frontline03 } from "./types/Frontline03";
import { Index as Frontline04Index, Frontline04 } from "./types/Frontline04";
import { Index as FurnitureCatalogCategoryIndex, FurnitureCatalogCategory } from "./types/FurnitureCatalogCategory";
import { Index as FurnitureCatalogItemListIndex, FurnitureCatalogItemList } from "./types/FurnitureCatalogItemList";
import { Index as GardeningSeedIndex, GardeningSeed } from "./types/GardeningSeed";
import { Index as GatheringConditionIndex, GatheringCondition } from "./types/GatheringCondition";
import { Index as GatheringExpIndex, GatheringExp } from "./types/GatheringExp";
import { Index as GatheringItemIndex, GatheringItem } from "./types/GatheringItem";
import { Index as GatheringItemLevelConvertTableIndex, GatheringItemLevelConvertTable } from "./types/GatheringItemLevelConvertTable";
import { Index as GatheringItemPointIndex, GatheringItemPoint } from "./types/GatheringItemPoint";
import { Index as GatheringLeveIndex, GatheringLeve } from "./types/GatheringLeve";
import { Index as GatheringLeveRouteIndex, GatheringLeveRoute } from "./types/GatheringLeveRoute";
import { Index as GatheringLeveRuleIndex, GatheringLeveRule } from "./types/GatheringLeveRule";
import { Index as GatheringNotebookListIndex, GatheringNotebookList } from "./types/GatheringNotebookList";
import { Index as GatheringPointIndex, GatheringPoint } from "./types/GatheringPoint";
import { Index as GatheringPointBaseIndex, GatheringPointBase } from "./types/GatheringPointBase";
import { Index as GatheringPointBonusIndex, GatheringPointBonus } from "./types/GatheringPointBonus";
import { Index as GatheringPointBonusTypeIndex, GatheringPointBonusType } from "./types/GatheringPointBonusType";
import { Index as GatheringPointNameIndex, GatheringPointName } from "./types/GatheringPointName";
import { Index as GatheringPointTransientIndex, GatheringPointTransient } from "./types/GatheringPointTransient";
import { Index as GatheringRarePopTimeTableIndex, GatheringRarePopTimeTable } from "./types/GatheringRarePopTimeTable";
import { Index as GatheringSubCategoryIndex, GatheringSubCategory } from "./types/GatheringSubCategory";
import { Index as GatheringTypeIndex, GatheringType } from "./types/GatheringType";
import { Index as GcArmyCaptureTacticsIndex, GcArmyCaptureTactics } from "./types/GcArmyCaptureTactics";
import { Index as GcArmyExpeditionIndex, GcArmyExpedition } from "./types/GcArmyExpedition";
import { Index as GcArmyExpeditionMemberBonusIndex, GcArmyExpeditionMemberBonus } from "./types/GcArmyExpeditionMemberBonus";
import { Index as GcArmyExpeditionTypeIndex, GcArmyExpeditionType } from "./types/GcArmyExpeditionType";
import { Index as GcArmyMemberGrowIndex, GcArmyMemberGrow } from "./types/GcArmyMemberGrow";
import { Index as GcArmyTrainingIndex, GcArmyTraining } from "./types/GcArmyTraining";
import { Index as GCRankGridaniaFemaleTextIndex, GCRankGridaniaFemaleText } from "./types/GCRankGridaniaFemaleText";
import { Index as GCRankGridaniaMaleTextIndex, GCRankGridaniaMaleText } from "./types/GCRankGridaniaMaleText";
import { Index as GCRankLimsaFemaleTextIndex, GCRankLimsaFemaleText } from "./types/GCRankLimsaFemaleText";
import { Index as GCRankLimsaMaleTextIndex, GCRankLimsaMaleText } from "./types/GCRankLimsaMaleText";
import { Index as GCRankUldahFemaleTextIndex, GCRankUldahFemaleText } from "./types/GCRankUldahFemaleText";
import { Index as GCRankUldahMaleTextIndex, GCRankUldahMaleText } from "./types/GCRankUldahMaleText";
import { Index as GCScripShopCategoryIndex, GCScripShopCategory } from "./types/GCScripShopCategory";
import { Index as GCScripShopItemIndex, GCScripShopItem } from "./types/GCScripShopItem";
import { Index as GCShopIndex, GCShop } from "./types/GCShop";
import { Index as GCShopItemCategoryIndex, GCShopItemCategory } from "./types/GCShopItemCategory";
import { Index as GCSupplyDutyIndex, GCSupplyDuty } from "./types/GCSupplyDuty";
import { Index as GCSupplyDutyRewardIndex, GCSupplyDutyReward } from "./types/GCSupplyDutyReward";
import { Index as GeneralActionIndex, GeneralAction } from "./types/GeneralAction";
import { Index as GFATEIndex, GFATE } from "./types/GFATE";
import { Index as GFateClimbing2Index, GFateClimbing2 } from "./types/GFateClimbing2";
import { Index as GFateClimbing2ContentIndex, GFateClimbing2Content } from "./types/GFateClimbing2Content";
import { Index as GFateClimbing2TotemTypeIndex, GFateClimbing2TotemType } from "./types/GFateClimbing2TotemType";
import { Index as GFateRideShootingIndex, GFateRideShooting } from "./types/GFateRideShooting";
import { Index as GilShopIndex, GilShop } from "./types/GilShop";
import { Index as GilShopItemIndex, GilShopItem } from "./types/GilShopItem";
import { Index as GimmickAccessorIndex, GimmickAccessor } from "./types/GimmickAccessor";
import { Index as GimmickJumpIndex, GimmickJump } from "./types/GimmickJump";
import { Index as GimmickRectIndex, GimmickRect } from "./types/GimmickRect";
import { Index as GoldSaucerArcadeMachineIndex, GoldSaucerArcadeMachine } from "./types/GoldSaucerArcadeMachine";
import { Index as GoldSaucerTextDataIndex, GoldSaucerTextData } from "./types/GoldSaucerTextData";
import { Index as GrandCompanyIndex, GrandCompany } from "./types/GrandCompany";
import { Index as GrandCompanyRankIndex, GrandCompanyRank } from "./types/GrandCompanyRank";
import { Index as GroupPoseFrameIndex, GroupPoseFrame } from "./types/GroupPoseFrame";
import { Index as GroupPoseStampIndex, GroupPoseStamp } from "./types/GroupPoseStamp";
import { Index as GroupPoseStampCategoryIndex, GroupPoseStampCategory } from "./types/GroupPoseStampCategory";
import { Index as GuardianDeityIndex, GuardianDeity } from "./types/GuardianDeity";
import { Index as GuideIndex, Guide } from "./types/Guide";
import { Index as GuidePageIndex, GuidePage } from "./types/GuidePage";
import { Index as GuidePageStringIndex, GuidePageString } from "./types/GuidePageString";
import { Index as GuideTitleIndex, GuideTitle } from "./types/GuideTitle";
import { Index as GuildleveAssignmentIndex, GuildleveAssignment } from "./types/GuildleveAssignment";
import { Index as GuildleveAssignmentCategoryIndex, GuildleveAssignmentCategory } from "./types/GuildleveAssignmentCategory";
import { Index as GuildOrderIndex, GuildOrder } from "./types/GuildOrder";
import { Index as GuildOrderGuideIndex, GuildOrderGuide } from "./types/GuildOrderGuide";
import { Index as GuildOrderOfficerIndex, GuildOrderOfficer } from "./types/GuildOrderOfficer";
import { Index as HairMakeTypeIndex, HairMakeType } from "./types/HairMakeType";
import { Index as HouseRetainerPoseIndex, HouseRetainerPose } from "./types/HouseRetainerPose";
import { Index as HousingAethernetIndex, HousingAethernet } from "./types/HousingAethernet";
import { Index as HousingAppealIndex, HousingAppeal } from "./types/HousingAppeal";
import { Index as HousingEmploymentNpcListIndex, HousingEmploymentNpcList } from "./types/HousingEmploymentNpcList";
import { Index as HousingEmploymentNpcRaceIndex, HousingEmploymentNpcRace } from "./types/HousingEmploymentNpcRace";
import { Index as HousingExteriorIndex, HousingExterior } from "./types/HousingExterior";
import { Index as HousingFurnitureIndex, HousingFurniture } from "./types/HousingFurniture";
import { Index as HousingLandSetIndex, HousingLandSet } from "./types/HousingLandSet";
import { Index as HousingMapMarkerInfoIndex, HousingMapMarkerInfo } from "./types/HousingMapMarkerInfo";
import { Index as HousingMerchantPoseIndex, HousingMerchantPose } from "./types/HousingMerchantPose";
import { Index as HousingPlacementIndex, HousingPlacement } from "./types/HousingPlacement";
import { Index as HousingPresetIndex, HousingPreset } from "./types/HousingPreset";
import { Index as HousingUnitedExteriorIndex, HousingUnitedExterior } from "./types/HousingUnitedExterior";
import { Index as HousingYardObjectIndex, HousingYardObject } from "./types/HousingYardObject";
import { Index as HowToIndex, HowTo } from "./types/HowTo";
import { Index as HowToCategoryIndex, HowToCategory } from "./types/HowToCategory";
import { Index as HowToPageIndex, HowToPage } from "./types/HowToPage";
import { Index as HugeCraftworksNpcIndex, HugeCraftworksNpc } from "./types/HugeCraftworksNpc";
import { Index as HugeCraftworksRankIndex, HugeCraftworksRank } from "./types/HugeCraftworksRank";
import { Index as HWDAnnounceIndex, HWDAnnounce } from "./types/HWDAnnounce";
import { Index as HWDCrafterSupplyIndex, HWDCrafterSupply } from "./types/HWDCrafterSupply";
import { Index as HWDCrafterSupplyRewardIndex, HWDCrafterSupplyReward } from "./types/HWDCrafterSupplyReward";
import { Index as HWDDevLayerControlIndex, HWDDevLayerControl } from "./types/HWDDevLayerControl";
import { Index as HWDDevLevelUIIndex, HWDDevLevelUI } from "./types/HWDDevLevelUI";
import { Index as HWDDevLivelyIndex, HWDDevLively } from "./types/HWDDevLively";
import { Index as HWDDevProgressIndex, HWDDevProgress } from "./types/HWDDevProgress";
import { Index as HWDGathereInspectTermIndex, HWDGathereInspectTerm } from "./types/HWDGathereInspectTerm";
import { Index as HWDGathererInspectionIndex, HWDGathererInspection } from "./types/HWDGathererInspection";
import { Index as HWDGathererInspectionRewardIndex, HWDGathererInspectionReward } from "./types/HWDGathererInspectionReward";
import { Index as HWDInfoBoardArticleIndex, HWDInfoBoardArticle } from "./types/HWDInfoBoardArticle";
import { Index as HWDInfoBoardArticleTransientIndex, HWDInfoBoardArticleTransient } from "./types/HWDInfoBoardArticleTransient";
import { Index as HWDInfoBoardArticleTypeIndex, HWDInfoBoardArticleType } from "./types/HWDInfoBoardArticleType";
import { Index as HWDIntroductionIndex, HWDIntroduction } from "./types/HWDIntroduction";
import { Index as HWDLevelChangeDeceptionIndex, HWDLevelChangeDeception } from "./types/HWDLevelChangeDeception";
import { Index as HWDSharedGroupIndex, HWDSharedGroup } from "./types/HWDSharedGroup";
import { Index as HWDSharedGroupControlParamIndex, HWDSharedGroupControlParam } from "./types/HWDSharedGroupControlParam";
import { Index as IKDContentBonusIndex, IKDContentBonus } from "./types/IKDContentBonus";
import { Index as IKDFishParamIndex, IKDFishParam } from "./types/IKDFishParam";
import { Index as IKDRouteIndex, IKDRoute } from "./types/IKDRoute";
import { Index as IKDRouteTableIndex, IKDRouteTable } from "./types/IKDRouteTable";
import { Index as IKDSpotIndex, IKDSpot } from "./types/IKDSpot";
import { Index as InclusionShopIndex, InclusionShop } from "./types/InclusionShop";
import { Index as InclusionShopCategoryIndex, InclusionShopCategory } from "./types/InclusionShopCategory";
import { Index as InclusionShopSeriesIndex, InclusionShopSeries } from "./types/InclusionShopSeries";
import { Index as IndividualWeatherIndex, IndividualWeather } from "./types/IndividualWeather";
import { Index as InstanceContentIndex, InstanceContent } from "./types/InstanceContent";
import { Index as InstanceContentBuffIndex, InstanceContentBuff } from "./types/InstanceContentBuff";
import { Index as InstanceContentCSBonusIndex, InstanceContentCSBonus } from "./types/InstanceContentCSBonus";
import { Index as InstanceContentGuideIndex, InstanceContentGuide } from "./types/InstanceContentGuide";
import { Index as InstanceContentTextDataIndex, InstanceContentTextData } from "./types/InstanceContentTextData";
import { Index as ItemIndex, Item } from "./types/Item";
import { Index as ItemActionIndex, ItemAction } from "./types/ItemAction";
import { Index as ItemActionTelepoIndex, ItemActionTelepo } from "./types/ItemActionTelepo";
import { Index as ItemBarterCheckIndex } from "./types/ItemBarterCheck";
import { Index as ItemFoodIndex, ItemFood } from "./types/ItemFood";
import { Index as ItemLevelIndex, ItemLevel } from "./types/ItemLevel";
import { Index as ItemSearchCategoryIndex, ItemSearchCategory } from "./types/ItemSearchCategory";
import { Index as ItemSeriesIndex, ItemSeries } from "./types/ItemSeries";
import { Index as ItemSortCategoryIndex, ItemSortCategory } from "./types/ItemSortCategory";
import { Index as ItemSpecialBonusIndex, ItemSpecialBonus } from "./types/ItemSpecialBonus";
import { Index as ItemUICategoryIndex, ItemUICategory } from "./types/ItemUICategory";
import { Index as JobHudManualIndex, JobHudManual } from "./types/JobHudManual";
import { Index as JobHudManualPriorityIndex, JobHudManualPriority } from "./types/JobHudManualPriority";
import { Index as JournalCategoryIndex, JournalCategory } from "./types/JournalCategory";
import { Index as JournalGenreIndex, JournalGenre } from "./types/JournalGenre";
import { Index as JournalSectionIndex, JournalSection } from "./types/JournalSection";
import { Index as KnockbackIndex, Knockback } from "./types/Knockback";
import { Index as LegacyQuestIndex, LegacyQuest } from "./types/LegacyQuest";
import { Index as LeveIndex, Leve } from "./types/Leve";
import { Index as LeveAssignmentTypeIndex, LeveAssignmentType } from "./types/LeveAssignmentType";
import { Index as LeveClientIndex, LeveClient } from "./types/LeveClient";
import { Index as LevelIndex, Level } from "./types/Level";
import { Index as LeveRewardItemIndex, LeveRewardItem } from "./types/LeveRewardItem";
import { Index as LeveRewardItemGroupIndex, LeveRewardItemGroup } from "./types/LeveRewardItemGroup";
import { Index as LeveStringIndex, LeveString } from "./types/LeveString";
import { Index as LeveVfxIndex, LeveVfx } from "./types/LeveVfx";
import { Index as LobbyIndex, Lobby } from "./types/Lobby";
import { Index as LogFilterIndex, LogFilter } from "./types/LogFilter";
import { Index as LogKindIndex, LogKind } from "./types/LogKind";
import { Index as LogKindCategoryTextIndex, LogKindCategoryText } from "./types/LogKindCategoryText";
import { Index as LogMessageIndex, LogMessage } from "./types/LogMessage";
import { Index as LotteryExchangeShopIndex, LotteryExchangeShop } from "./types/LotteryExchangeShop";
import { Index as MacroIconIndex, MacroIcon } from "./types/MacroIcon";
import { Index as MacroIconRedirectOldIndex, MacroIconRedirectOld } from "./types/MacroIconRedirectOld";
import { Index as MainCommandIndex, MainCommand } from "./types/MainCommand";
import { Index as MainCommandCategoryIndex, MainCommandCategory } from "./types/MainCommandCategory";
import { Index as ManeuversArmorIndex, ManeuversArmor } from "./types/ManeuversArmor";
import { Index as MapIndex, Map } from "./types/Map";
import { Index as MapMarkerIndex, MapMarker } from "./types/MapMarker";
import { Index as MapMarkerRegionIndex, MapMarkerRegion } from "./types/MapMarkerRegion";
import { Index as MapSymbolIndex, MapSymbol } from "./types/MapSymbol";
import { Index as MarkerIndex, Marker } from "./types/Marker";
import { Index as MasterpieceSupplyDutyIndex, MasterpieceSupplyDuty } from "./types/MasterpieceSupplyDuty";
import { Index as MasterpieceSupplyMultiplierIndex, MasterpieceSupplyMultiplier } from "./types/MasterpieceSupplyMultiplier";
import { Index as MateriaIndex, Materia } from "./types/Materia";
import { Index as MateriaJoinRateIndex, MateriaJoinRate } from "./types/MateriaJoinRate";
import { Index as MateriaJoinRateGatherCraftIndex, MateriaJoinRateGatherCraft } from "./types/MateriaJoinRateGatherCraft";
import { Index as MateriaTomestoneRateIndex, MateriaTomestoneRate } from "./types/MateriaTomestoneRate";
import { Index as MiniGameRAIndex, MiniGameRA } from "./types/MiniGameRA";
import { Index as MinionRaceIndex, MinionRace } from "./types/MinionRace";
import { Index as MinionRulesIndex, MinionRules } from "./types/MinionRules";
import { Index as MinionSkillTypeIndex, MinionSkillType } from "./types/MinionSkillType";
import { Index as MobHuntOrderIndex, MobHuntOrder } from "./types/MobHuntOrder";
import { Index as MobHuntOrderTypeIndex, MobHuntOrderType } from "./types/MobHuntOrderType";
import { Index as MobHuntRewardIndex, MobHuntReward } from "./types/MobHuntReward";
import { Index as MobHuntRewardCapIndex, MobHuntRewardCap } from "./types/MobHuntRewardCap";
import { Index as MobHuntTargetIndex, MobHuntTarget } from "./types/MobHuntTarget";
import { Index as ModelCharaIndex, ModelChara } from "./types/ModelChara";
import { Index as ModelSkeletonIndex, ModelSkeleton } from "./types/ModelSkeleton";
import { Index as ModelStateIndex, ModelState } from "./types/ModelState";
import { Index as MonsterNoteIndex, MonsterNote } from "./types/MonsterNote";
import { Index as MonsterNoteTargetIndex, MonsterNoteTarget } from "./types/MonsterNoteTarget";
import { Index as MotionTimelineIndex, MotionTimeline } from "./types/MotionTimeline";
import { Index as MotionTimelineBlendTableIndex, MotionTimelineBlendTable } from "./types/MotionTimelineBlendTable";
import { Index as MountIndex, Mount } from "./types/Mount";
import { Index as MountActionIndex, MountAction } from "./types/MountAction";
import { Index as MountCustomizeIndex, MountCustomize } from "./types/MountCustomize";
import { Index as MountFlyingConditionIndex, MountFlyingCondition } from "./types/MountFlyingCondition";
import { Index as MountSpeedIndex, MountSpeed } from "./types/MountSpeed";
import { Index as MountTransientIndex, MountTransient } from "./types/MountTransient";
import { Index as MoveTimelineIndex, MoveTimeline } from "./types/MoveTimeline";
import { Index as MoveVfxIndex, MoveVfx } from "./types/MoveVfx";
import { Index as MovieSubtitleIndex, MovieSubtitle } from "./types/MovieSubtitle";
import { Index as MovieSubtitle500Index, MovieSubtitle500 } from "./types/MovieSubtitle500";
import { Index as MovieSubtitleVoyageIndex, MovieSubtitleVoyage } from "./types/MovieSubtitleVoyage";
import { Index as NotebookDivisionIndex, NotebookDivision } from "./types/NotebookDivision";
import { Index as NotebookDivisionCategoryIndex, NotebookDivisionCategory } from "./types/NotebookDivisionCategory";
import { Index as NotoriousMonsterIndex, NotoriousMonster } from "./types/NotoriousMonster";
import { Index as NpcEquipIndex, NpcEquip } from "./types/NpcEquip";
import { Index as NpcYellIndex, NpcYell } from "./types/NpcYell";
import { Index as OmenIndex, Omen } from "./types/Omen";
import { Index as OnlineStatusIndex, OnlineStatus } from "./types/OnlineStatus";
import { Index as OpenContentIndex, OpenContent } from "./types/OpenContent";
import { Index as OpenContentCandidateNameIndex, OpenContentCandidateName } from "./types/OpenContentCandidateName";
import { Index as OpeningIndex, Opening } from "./types/Opening";
import { Index as OrchestrionIndex, Orchestrion } from "./types/Orchestrion";
import { Index as OrchestrionCategoryIndex, OrchestrionCategory } from "./types/OrchestrionCategory";
import { Index as OrchestrionPathIndex, OrchestrionPath } from "./types/OrchestrionPath";
import { Index as OrchestrionUiparamIndex, OrchestrionUiparam } from "./types/OrchestrionUiparam";
import { Index as ParamGrowIndex, ParamGrow } from "./types/ParamGrow";
import { Index as PartyContentIndex, PartyContent } from "./types/PartyContent";
import { Index as PartyContentCutsceneIndex, PartyContentCutscene } from "./types/PartyContentCutscene";
import { Index as PartyContentTextDataIndex, PartyContentTextData } from "./types/PartyContentTextData";
import { Index as PatchMarkIndex, PatchMark } from "./types/PatchMark";
import { Index as PerformIndex, Perform } from "./types/Perform";
import { Index as PerformTransientIndex, PerformTransient } from "./types/PerformTransient";
import { Index as PetIndex, Pet } from "./types/Pet";
import { Index as PetActionIndex, PetAction } from "./types/PetAction";
import { Index as PhysicsGroupIndex, PhysicsGroup } from "./types/PhysicsGroup";
import { Index as PhysicsWindIndex, PhysicsWind } from "./types/PhysicsWind";
import { Index as PictureIndex, Picture } from "./types/Picture";
import { Index as PlaceNameIndex, PlaceName } from "./types/PlaceName";
import { Index as PlantPotFlowerSeedIndex, PlantPotFlowerSeed } from "./types/PlantPotFlowerSeed";
import { Index as PreHandlerIndex, PreHandler } from "./types/PreHandler";
import { Index as PresetCameraIndex, PresetCamera } from "./types/PresetCamera";
import { Index as PresetCameraAdjustIndex, PresetCameraAdjust } from "./types/PresetCameraAdjust";
import { Index as PublicContentIndex, PublicContent } from "./types/PublicContent";
import { Index as PublicContentCutsceneIndex, PublicContentCutscene } from "./types/PublicContentCutscene";
import { Index as PublicContentTextDataIndex, PublicContentTextData } from "./types/PublicContentTextData";
import { Index as PurifyIndex, Purify } from "./types/Purify";
import { Index as PvPActionIndex, PvPAction } from "./types/PvPAction";
import { Index as PvPActionSortIndex, PvPActionSort } from "./types/PvPActionSort";
import { Index as PvPRankIndex, PvPRank } from "./types/PvPRank";
import { Index as PvPSelectTraitIndex, PvPSelectTrait } from "./types/PvPSelectTrait";
import { Index as PvPTraitIndex, PvPTrait } from "./types/PvPTrait";
import { Index as QuestIndex, Quest } from "./types/Quest";
import { Index as QuestBattleIndex, QuestBattle } from "./types/QuestBattle";
import { Index as QuestChapterIndex, QuestChapter } from "./types/QuestChapter";
import { Index as QuestClassJobRewardIndex, QuestClassJobReward } from "./types/QuestClassJobReward";
import { Index as QuestClassJobSupplyIndex, QuestClassJobSupply } from "./types/QuestClassJobSupply";
import { Index as QuestDerivedClassIndex, QuestDerivedClass } from "./types/QuestDerivedClass";
import { Index as QuestRedoIndex, QuestRedo } from "./types/QuestRedo";
import { Index as QuestRedoChapterUIIndex, QuestRedoChapterUI } from "./types/QuestRedoChapterUI";
import { Index as QuestRedoChapterUICategoryIndex, QuestRedoChapterUICategory } from "./types/QuestRedoChapterUICategory";
import { Index as QuestRedoChapterUITabIndex, QuestRedoChapterUITab } from "./types/QuestRedoChapterUITab";
import { Index as QuestRedoGroupIndex, QuestRedoGroup } from "./types/QuestRedoGroup";
import { Index as QuestRedoIncompChapterIndex, QuestRedoIncompChapter } from "./types/QuestRedoIncompChapter";
import { Index as QuestRepeatFlagIndex, QuestRepeatFlag } from "./types/QuestRepeatFlag";
import { Index as QuestRewardOtherIndex, QuestRewardOther } from "./types/QuestRewardOther";
import { Index as QuickChatIndex, QuickChat } from "./types/QuickChat";
import { Index as QuickChatTransientIndex, QuickChatTransient } from "./types/QuickChatTransient";
import { Index as RaceIndex, Race } from "./types/Race";
import { Index as RacingChocoboItemIndex, RacingChocoboItem } from "./types/RacingChocoboItem";
import { Index as RacingChocoboNameIndex, RacingChocoboName } from "./types/RacingChocoboName";
import { Index as RacingChocoboNameCategoryIndex, RacingChocoboNameCategory } from "./types/RacingChocoboNameCategory";
import { Index as RacingChocoboNameInfoIndex, RacingChocoboNameInfo } from "./types/RacingChocoboNameInfo";
import { Index as RacingChocoboParamIndex, RacingChocoboParam } from "./types/RacingChocoboParam";
import { Index as RecastNavimeshIndex, RecastNavimesh } from "./types/RecastNavimesh";
import { Index as RecipeIndex, Recipe } from "./types/Recipe";
import { Index as RecipeLevelTableIndex, RecipeLevelTable } from "./types/RecipeLevelTable";
import { Index as RecipeLookupIndex, RecipeLookup } from "./types/RecipeLookup";
import { Index as RecipeNotebookListIndex, RecipeNotebookList } from "./types/RecipeNotebookList";
import { Index as RecommendContentsIndex, RecommendContents } from "./types/RecommendContents";
import { Index as RelicIndex, Relic } from "./types/Relic";
import { Index as Relic3Index, Relic3 } from "./types/Relic3";
import { Index as RelicItemIndex, RelicItem } from "./types/RelicItem";
import { Index as RelicNoteIndex, RelicNote } from "./types/RelicNote";
import { Index as RelicNoteCategoryIndex, RelicNoteCategory } from "./types/RelicNoteCategory";
import { Index as ResidentIndex, Resident } from "./types/Resident";
import { Index as RetainerTaskIndex, RetainerTask } from "./types/RetainerTask";
import { Index as RetainerTaskLvRangeIndex, RetainerTaskLvRange } from "./types/RetainerTaskLvRange";
import { Index as RetainerTaskNormalIndex, RetainerTaskNormal } from "./types/RetainerTaskNormal";
import { Index as RetainerTaskParameterIndex, RetainerTaskParameter } from "./types/RetainerTaskParameter";
import { Index as RetainerTaskRandomIndex, RetainerTaskRandom } from "./types/RetainerTaskRandom";
import { Index as RideShootingIndex, RideShooting } from "./types/RideShooting";
import { Index as RideShootingTextDataIndex, RideShootingTextData } from "./types/RideShootingTextData";
import { Index as RPParameterIndex, RPParameter } from "./types/RPParameter";
import { Index as SalvageIndex, Salvage } from "./types/Salvage";
import { Index as SatisfactionArbitrationIndex, SatisfactionArbitration } from "./types/SatisfactionArbitration";
import { Index as SatisfactionNpcIndex, SatisfactionNpc } from "./types/SatisfactionNpc";
import { Index as SatisfactionSupplyIndex, SatisfactionSupply } from "./types/SatisfactionSupply";
import { Index as SatisfactionSupplyRewardIndex, SatisfactionSupplyReward } from "./types/SatisfactionSupplyReward";
import { Index as ScenarioTreeIndex, ScenarioTree } from "./types/ScenarioTree";
import { Index as ScenarioTreeTipsIndex, ScenarioTreeTips } from "./types/ScenarioTreeTips";
import { Index as ScenarioTreeTipsClassQuestIndex, ScenarioTreeTipsClassQuest } from "./types/ScenarioTreeTipsClassQuest";
import { Index as ScenarioTreeTipsQuestIndex, ScenarioTreeTipsQuest } from "./types/ScenarioTreeTipsQuest";
import { Index as ScenarioTypeIndex, ScenarioType } from "./types/ScenarioType";
import { Index as ScreenImageIndex, ScreenImage } from "./types/ScreenImage";
import { Index as SecretRecipeBookIndex, SecretRecipeBook } from "./types/SecretRecipeBook";
import { Index as SkyIsland2MissionIndex, SkyIsland2Mission } from "./types/SkyIsland2Mission";
import { Index as SkyIsland2MissionDetailIndex, SkyIsland2MissionDetail } from "./types/SkyIsland2MissionDetail";
import { Index as SkyIsland2MissionTypeIndex, SkyIsland2MissionType } from "./types/SkyIsland2MissionType";
import { Index as SkyIsland2RangeTypeIndex, SkyIsland2RangeType } from "./types/SkyIsland2RangeType";
import { Index as SnipeTalkIndex, SnipeTalk } from "./types/SnipeTalk";
import { Index as SnipeTalkNameIndex, SnipeTalkName } from "./types/SnipeTalkName";
import { Index as SpearfishingItemIndex, SpearfishingItem } from "./types/SpearfishingItem";
import { Index as SpearfishingNotebookIndex, SpearfishingNotebook } from "./types/SpearfishingNotebook";
import { Index as SpearfishingRecordPageIndex, SpearfishingRecordPage } from "./types/SpearfishingRecordPage";
import { Index as SpecialShopIndex, SpecialShop } from "./types/SpecialShop";
import { Index as SpecialShopItemCategoryIndex, SpecialShopItemCategory } from "./types/SpecialShopItemCategory";
import { Index as StainIndex, Stain } from "./types/Stain";
import { Index as StainTransientIndex, StainTransient } from "./types/StainTransient";
import { Index as StatusIndex, Status } from "./types/Status";
import { Index as StatusHitEffectIndex, StatusHitEffect } from "./types/StatusHitEffect";
import { Index as StatusLoopVFXIndex, StatusLoopVFX } from "./types/StatusLoopVFX";
import { Index as StoryIndex, Story } from "./types/Story";
import { Index as SubmarineExplorationIndex, SubmarineExploration } from "./types/SubmarineExploration";
import { Index as SubmarineMapIndex, SubmarineMap } from "./types/SubmarineMap";
import { Index as SubmarinePartIndex, SubmarinePart } from "./types/SubmarinePart";
import { Index as SubmarineRankIndex, SubmarineRank } from "./types/SubmarineRank";
import { Index as SwitchTalkIndex, SwitchTalk } from "./types/SwitchTalk";
import { Index as SwitchTalkVariationIndex, SwitchTalkVariation } from "./types/SwitchTalkVariation";
import { Index as TerritoryTypeIndex, TerritoryType } from "./types/TerritoryType";
import { Index as TerritoryTypeTransientIndex, TerritoryTypeTransient } from "./types/TerritoryTypeTransient";
import { Index as TextCommandIndex, TextCommand } from "./types/TextCommand";
import { Index as TitleIndex, Title } from "./types/Title";
import { Index as TomestonesIndex, Tomestones } from "./types/Tomestones";
import { Index as TomestonesItemIndex, TomestonesItem } from "./types/TomestonesItem";
import { Index as TopicSelectIndex, TopicSelect } from "./types/TopicSelect";
import { Index as TownIndex, Town } from "./types/Town";
import { Index as TraitIndex, Trait } from "./types/Trait";
import { Index as TraitRecastIndex, TraitRecast } from "./types/TraitRecast";
import { Index as TraitTransientIndex, TraitTransient } from "./types/TraitTransient";
import { Index as TransformationIndex, Transformation } from "./types/Transformation";
import { Index as TreasureIndex, Treasure } from "./types/Treasure";
import { Index as TreasureHuntRankIndex, TreasureHuntRank } from "./types/TreasureHuntRank";
import { Index as TreasureModelIndex, TreasureModel } from "./types/TreasureModel";
import { Index as TreasureSpotIndex, TreasureSpot } from "./types/TreasureSpot";
import { Index as TribeIndex, Tribe } from "./types/Tribe";
import { Index as TripleTriadIndex, TripleTriad } from "./types/TripleTriad";
import { Index as TripleTriadCardIndex, TripleTriadCard } from "./types/TripleTriadCard";
import { Index as TripleTriadCardRarityIndex, TripleTriadCardRarity } from "./types/TripleTriadCardRarity";
import { Index as TripleTriadCardResidentIndex, TripleTriadCardResident } from "./types/TripleTriadCardResident";
import { Index as TripleTriadCardTypeIndex, TripleTriadCardType } from "./types/TripleTriadCardType";
import { Index as TripleTriadCompetitionIndex, TripleTriadCompetition } from "./types/TripleTriadCompetition";
import { Index as TripleTriadRuleIndex, TripleTriadRule } from "./types/TripleTriadRule";
import { Index as TutorialIndex, Tutorial } from "./types/Tutorial";
import { Index as TutorialDPSIndex, TutorialDPS } from "./types/TutorialDPS";
import { Index as TutorialHealerIndex, TutorialHealer } from "./types/TutorialHealer";
import { Index as TutorialTankIndex, TutorialTank } from "./types/TutorialTank";
import { Index as UIColorIndex, UIColor } from "./types/UIColor";
import { Index as VaseFlowerIndex, VaseFlower } from "./types/VaseFlower";
import { Index as VFXIndex, VFX } from "./types/VFX";
import { Index as WarpIndex, Warp } from "./types/Warp";
import { Index as WarpConditionIndex, WarpCondition } from "./types/WarpCondition";
import { Index as WarpLogicIndex, WarpLogic } from "./types/WarpLogic";
import { Index as WeaponTimelineIndex, WeaponTimeline } from "./types/WeaponTimeline";
import { Index as WeatherIndex, Weather } from "./types/Weather";
import { Index as WeatherGroupIndex, WeatherGroup } from "./types/WeatherGroup";
import { Index as WeatherRateIndex, WeatherRate } from "./types/WeatherRate";
import { Index as WeatherReportReplaceIndex, WeatherReportReplace } from "./types/WeatherReportReplace";
import { Index as WeddingBGMIndex, WeddingBGM } from "./types/WeddingBGM";
import { Index as WeeklyBingoOrderDataIndex, WeeklyBingoOrderData } from "./types/WeeklyBingoOrderData";
import { Index as WeeklyBingoRewardDataIndex, WeeklyBingoRewardData } from "./types/WeeklyBingoRewardData";
import { Index as WeeklyBingoTextIndex, WeeklyBingoText } from "./types/WeeklyBingoText";
import { Index as WeeklyLotBonusIndex, WeeklyLotBonus } from "./types/WeeklyLotBonus";
import { Index as WorldIndex, World } from "./types/World";
import { Index as WorldDCGroupTypeIndex, WorldDCGroupType } from "./types/WorldDCGroupType";
import { Index as YardCatalogCategoryIndex, YardCatalogCategory } from "./types/YardCatalogCategory";
import { Index as YardCatalogItemListIndex, YardCatalogItemList } from "./types/YardCatalogItemList";
import { Index as YKWIndex, YKW } from "./types/YKW";
import { Index as ZoneSharedGroupIndex, ZoneSharedGroup } from "./types/ZoneSharedGroup";
export type IndexPageFor<T> =
  T extends "Achievement" ? AchievementIndex :
  T extends "AchievementCategory" ? AchievementCategoryIndex :
  T extends "AchievementHideCondition" ? AchievementHideConditionIndex :
  T extends "AchievementKind" ? AchievementKindIndex :
  T extends "Action" ? ActionIndex :
  T extends "ActionCastTimeline" ? ActionCastTimelineIndex :
  T extends "ActionCastVFX" ? ActionCastVFXIndex :
  T extends "ActionCategory" ? ActionCategoryIndex :
  T extends "ActionComboRoute" ? ActionComboRouteIndex :
  T extends "ActionIndirection" ? ActionIndirectionIndex :
  T extends "ActionParam" ? ActionParamIndex :
  T extends "ActionProcStatus" ? ActionProcStatusIndex :
  T extends "ActionTimeline" ? ActionTimelineIndex :
  T extends "ActionTimelineMove" ? ActionTimelineMoveIndex :
  T extends "ActionTimelineReplace" ? ActionTimelineReplaceIndex :
  T extends "ActionTransient" ? ActionTransientIndex :
  T extends "ActivityFeedButtons" ? ActivityFeedButtonsIndex :
  T extends "ActivityFeedCaptions" ? ActivityFeedCaptionsIndex :
  T extends "ActivityFeedGroupCaptions" ? ActivityFeedGroupCaptionsIndex :
  T extends "ActivityFeedImages" ? ActivityFeedImagesIndex :
  T extends "Addon" ? AddonIndex :
  T extends "AddonHud" ? AddonHudIndex :
  T extends "Adventure" ? AdventureIndex :
  T extends "AdventureExPhase" ? AdventureExPhaseIndex :
  T extends "AetherCurrent" ? AetherCurrentIndex :
  T extends "AetherCurrentCompFlgSet" ? AetherCurrentCompFlgSetIndex :
  T extends "AetherialWheel" ? AetherialWheelIndex :
  T extends "Aetheryte" ? AetheryteIndex :
  T extends "AetheryteSystemDefine" ? AetheryteSystemDefineIndex :
  T extends "AirshipExplorationLevel" ? AirshipExplorationLevelIndex :
  T extends "AirshipExplorationLog" ? AirshipExplorationLogIndex :
  T extends "AirshipExplorationParamType" ? AirshipExplorationParamTypeIndex :
  T extends "AirshipExplorationPart" ? AirshipExplorationPartIndex :
  T extends "AirshipExplorationPoint" ? AirshipExplorationPointIndex :
  T extends "AnimationLOD" ? AnimationLODIndex :
  T extends "AnimaWeapon5" ? AnimaWeapon5Index :
  T extends "AnimaWeapon5Param" ? AnimaWeapon5ParamIndex :
  T extends "AnimaWeapon5PatternGroup" ? AnimaWeapon5PatternGroupIndex :
  T extends "AnimaWeapon5SpiritTalk" ? AnimaWeapon5SpiritTalkIndex :
  T extends "AnimaWeapon5SpiritTalkParam" ? AnimaWeapon5SpiritTalkParamIndex :
  T extends "AnimaWeapon5TradeItem" ? AnimaWeapon5TradeItemIndex :
  T extends "AnimaWeaponFUITalk" ? AnimaWeaponFUITalkIndex :
  T extends "AnimaWeaponFUITalkParam" ? AnimaWeaponFUITalkParamIndex :
  T extends "AnimaWeaponIcon" ? AnimaWeaponIconIndex :
  T extends "AnimaWeaponItem" ? AnimaWeaponItemIndex :
  T extends "AozAction" ? AozActionIndex :
  T extends "AozActionTransient" ? AozActionTransientIndex :
  T extends "AOZArrangement" ? AOZArrangementIndex :
  T extends "AOZBoss" ? AOZBossIndex :
  T extends "AOZContent" ? AOZContentIndex :
  T extends "AOZContentBriefingBNpc" ? AOZContentBriefingBNpcIndex :
  T extends "AquariumFish" ? AquariumFishIndex :
  T extends "AquariumWater" ? AquariumWaterIndex :
  T extends "ArrayEventHandler" ? ArrayEventHandlerIndex :
  T extends "AttackType" ? AttackTypeIndex :
  T extends "BacklightColor" ? BacklightColorIndex :
  T extends "Ballista" ? BallistaIndex :
  T extends "Balloon" ? BalloonIndex :
  T extends "BaseParam" ? BaseParamIndex :
  T extends "BattleLeve" ? BattleLeveIndex :
  T extends "BattleLeveRule" ? BattleLeveRuleIndex :
  T extends "BeastRankBonus" ? BeastRankBonusIndex :
  T extends "BeastReputationRank" ? BeastReputationRankIndex :
  T extends "BeastTribe" ? BeastTribeIndex :
  T extends "Behavior" ? BehaviorIndex :
  T extends "BehaviorPath" ? BehaviorPathIndex :
  T extends "BenchmarkOverrideEquipment" ? BenchmarkOverrideEquipmentIndex :
  T extends "BGM" ? BGMIndex :
  T extends "BGMFade" ? BGMFadeIndex :
  T extends "BGMFadeType" ? BGMFadeTypeIndex :
  T extends "BGMScene" ? BGMSceneIndex :
  T extends "BGMSituation" ? BGMSituationIndex :
  T extends "BGMSwitch" ? BGMSwitchIndex :
  T extends "BGMSystemDefine" ? BGMSystemDefineIndex :
  T extends "BNpcAnnounceIcon" ? BNpcAnnounceIconIndex :
  T extends "BNpcBase" ? BNpcBaseIndex :
  T extends "BNpcCustomize" ? BNpcCustomizeIndex :
  T extends "BNpcName" ? BNpcNameIndex :
  T extends "BNpcParts" ? BNpcPartsIndex :
  T extends "BNpcState" ? BNpcStateIndex :
  T extends "Buddy" ? BuddyIndex :
  T extends "BuddyAction" ? BuddyActionIndex :
  T extends "BuddyEquip" ? BuddyEquipIndex :
  T extends "BuddyItem" ? BuddyItemIndex :
  T extends "BuddyRank" ? BuddyRankIndex :
  T extends "BuddySkill" ? BuddySkillIndex :
  T extends "Cabinet" ? CabinetIndex :
  T extends "CabinetCategory" ? CabinetCategoryIndex :
  T extends "Calendar" ? CalendarIndex :
  T extends "Carry" ? CarryIndex :
  T extends "Channeling" ? ChannelingIndex :
  T extends "CharaMakeClassEquip" ? CharaMakeClassEquipIndex :
  T extends "CharaMakeCustomize" ? CharaMakeCustomizeIndex :
  T extends "CharaMakeName" ? CharaMakeNameIndex :
  T extends "CharaMakeType" ? CharaMakeTypeIndex :
  T extends "ChocoboRace" ? ChocoboRaceIndex :
  T extends "ChocoboRaceAbility" ? ChocoboRaceAbilityIndex :
  T extends "ChocoboRaceAbilityType" ? ChocoboRaceAbilityTypeIndex :
  T extends "ChocoboRaceItem" ? ChocoboRaceItemIndex :
  T extends "ChocoboRaceRank" ? ChocoboRaceRankIndex :
  T extends "ChocoboRaceStatus" ? ChocoboRaceStatusIndex :
  T extends "ChocoboRaceTerritory" ? ChocoboRaceTerritoryIndex :
  T extends "ChocoboRaceTutorial" ? ChocoboRaceTutorialIndex :
  T extends "ChocoboRaceWeather" ? ChocoboRaceWeatherIndex :
  T extends "ChocoboTaxi" ? ChocoboTaxiIndex :
  T extends "ChocoboTaxiStand" ? ChocoboTaxiStandIndex :
  T extends "CircleActivity" ? CircleActivityIndex :
  T extends "ClassJob" ? ClassJobIndex :
  T extends "ClassJobCategory" ? ClassJobCategoryIndex :
  T extends "CollectablesShop" ? CollectablesShopIndex :
  T extends "CollectablesShopItem" ? CollectablesShopItemIndex :
  T extends "CollectablesShopItemGroup" ? CollectablesShopItemGroupIndex :
  T extends "CollectablesShopRefine" ? CollectablesShopRefineIndex :
  T extends "CollectablesShopRewardItem" ? CollectablesShopRewardItemIndex :
  T extends "CollectablesShopRewardScrip" ? CollectablesShopRewardScripIndex :
  T extends "Companion" ? CompanionIndex :
  T extends "CompanionMove" ? CompanionMoveIndex :
  T extends "CompanionTransient" ? CompanionTransientIndex :
  T extends "CompanyAction" ? CompanyActionIndex :
  T extends "CompanyCraftDraft" ? CompanyCraftDraftIndex :
  T extends "CompanyCraftDraftCategory" ? CompanyCraftDraftCategoryIndex :
  T extends "CompanyCraftManufactoryState" ? CompanyCraftManufactoryStateIndex :
  T extends "CompanyCraftPart" ? CompanyCraftPartIndex :
  T extends "CompanyCraftProcess" ? CompanyCraftProcessIndex :
  T extends "CompanyCraftSequence" ? CompanyCraftSequenceIndex :
  T extends "CompanyCraftSupplyItem" ? CompanyCraftSupplyItemIndex :
  T extends "CompanyCraftType" ? CompanyCraftTypeIndex :
  T extends "CompanyLeve" ? CompanyLeveIndex :
  T extends "CompanyLeveRule" ? CompanyLeveRuleIndex :
  T extends "CompleteJournal" ? CompleteJournalIndex :
  T extends "CompleteJournalCategory" ? CompleteJournalCategoryIndex :
  T extends "Condition" ? ConditionIndex :
  T extends "ConfigKey" ? ConfigKeyIndex :
  T extends "ContentCloseCycle" ? ContentCloseCycleIndex :
  T extends "ContentExAction" ? ContentExActionIndex :
  T extends "ContentFinderCondition" ? ContentFinderConditionIndex :
  T extends "ContentFinderConditionTransient" ? ContentFinderConditionTransientIndex :
  T extends "ContentGauge" ? ContentGaugeIndex :
  T extends "ContentGaugeColor" ? ContentGaugeColorIndex :
  T extends "ContentMemberType" ? ContentMemberTypeIndex :
  T extends "ContentNpcTalk" ? ContentNpcTalkIndex :
  T extends "ContentRandomSelect" ? ContentRandomSelectIndex :
  T extends "ContentRoulette" ? ContentRouletteIndex :
  T extends "ContentRouletteOpenRule" ? ContentRouletteOpenRuleIndex :
  T extends "ContentRouletteRoleBonus" ? ContentRouletteRoleBonusIndex :
  T extends "ContentsNote" ? ContentsNoteIndex :
  T extends "ContentTalk" ? ContentTalkIndex :
  T extends "ContentTalkParam" ? ContentTalkParamIndex :
  T extends "ContentType" ? ContentTypeIndex :
  T extends "CraftAction" ? CraftActionIndex :
  T extends "CraftLeve" ? CraftLeveIndex :
  T extends "CraftLevelDifference" ? CraftLevelDifferenceIndex :
  T extends "CraftType" ? CraftTypeIndex :
  T extends "Credit" ? CreditIndex :
  T extends "CreditBackImage" ? CreditBackImageIndex :
  T extends "CreditCast" ? CreditCastIndex :
  T extends "CreditList" ? CreditListIndex :
  T extends "CreditListText" ? CreditListTextIndex :
  T extends "Currency" ? CurrencyIndex :
  T extends "CustomTalk" ? CustomTalkIndex :
  T extends "CustomTalkDynamicIcon" ? CustomTalkDynamicIconIndex :
  T extends "CustomTalkNestHandlers" ? CustomTalkNestHandlersIndex :
  T extends "Cutscene" ? CutsceneIndex :
  T extends "CutsceneMotion" ? CutsceneMotionIndex :
  T extends "CutsceneWorkIndex" ? CutsceneWorkIndexIndex :
  T extends "CutScreenImage" ? CutScreenImageIndex :
  T extends "CycleTime" ? CycleTimeIndex :
  T extends "DailySupplyItem" ? DailySupplyItemIndex :
  T extends "DawnContent" ? DawnContentIndex :
  T extends "DawnGrowMember" ? DawnGrowMemberIndex :
  T extends "DawnMemberUIParam" ? DawnMemberUIParamIndex :
  T extends "DawnQuestAnnounce" ? DawnQuestAnnounceIndex :
  T extends "DawnQuestMember" ? DawnQuestMemberIndex :
  T extends "DeepDungeon" ? DeepDungeonIndex :
  T extends "DeepDungeonBan" ? DeepDungeonBanIndex :
  T extends "DeepDungeonDanger" ? DeepDungeonDangerIndex :
  T extends "DeepDungeonEquipment" ? DeepDungeonEquipmentIndex :
  T extends "DeepDungeonFloorEffectUI" ? DeepDungeonFloorEffectUIIndex :
  T extends "DeepDungeonItem" ? DeepDungeonItemIndex :
  T extends "DeepDungeonLayer" ? DeepDungeonLayerIndex :
  T extends "DeepDungeonMagicStone" ? DeepDungeonMagicStoneIndex :
  T extends "DeepDungeonMap5X" ? DeepDungeonMap5XIndex :
  T extends "DeepDungeonRoom" ? DeepDungeonRoomIndex :
  T extends "DeepDungeonStatus" ? DeepDungeonStatusIndex :
  T extends "DefaultTalk" ? DefaultTalkIndex :
  T extends "DefaultTalkLipSyncType" ? DefaultTalkLipSyncTypeIndex :
  T extends "DeliveryQuest" ? DeliveryQuestIndex :
  T extends "Description" ? DescriptionIndex :
  T extends "DescriptionPage" ? DescriptionPageIndex :
  T extends "DescriptionSection" ? DescriptionSectionIndex :
  T extends "DescriptionString" ? DescriptionStringIndex :
  T extends "DisposalShop" ? DisposalShopIndex :
  T extends "DisposalShopFilterType" ? DisposalShopFilterTypeIndex :
  T extends "DisposalShopItem" ? DisposalShopItemIndex :
  T extends "DpsChallenge" ? DpsChallengeIndex :
  T extends "DpsChallengeOfficer" ? DpsChallengeOfficerIndex :
  T extends "DpsChallengeTransient" ? DpsChallengeTransientIndex :
  T extends "EmjAddon" ? EmjAddonIndex :
  T extends "EmjDani" ? EmjDaniIndex :
  T extends "Emote" ? EmoteIndex :
  T extends "EmoteCategory" ? EmoteCategoryIndex :
  T extends "EmoteMode" ? EmoteModeIndex :
  T extends "ENpcBase" ? ENpcBaseIndex :
  T extends "ENpcDressUp" ? ENpcDressUpIndex :
  T extends "ENpcDressUpDress" ? ENpcDressUpDressIndex :
  T extends "ENpcResident" ? ENpcResidentIndex :
  T extends "EObj" ? EObjIndex :
  T extends "EObjName" ? EObjNameIndex :
  T extends "EquipRaceCategory" ? EquipRaceCategoryIndex :
  T extends "EquipSlotCategory" ? EquipSlotCategoryIndex :
  T extends "EurekaAetherItem" ? EurekaAetherItemIndex :
  T extends "EurekaAethernet" ? EurekaAethernetIndex :
  T extends "EurekaGrowData" ? EurekaGrowDataIndex :
  T extends "EurekaLogosMixerProbability" ? EurekaLogosMixerProbabilityIndex :
  T extends "EurekaMagiaAction" ? EurekaMagiaActionIndex :
  T extends "EurekaMagiciteItem" ? EurekaMagiciteItemIndex :
  T extends "EurekaMagiciteItemType" ? EurekaMagiciteItemTypeIndex :
  T extends "EurekaSphereElementAdjust" ? EurekaSphereElementAdjustIndex :
  T extends "EventAction" ? EventActionIndex :
  T extends "EventIconPriority" ? EventIconPriorityIndex :
  T extends "EventIconType" ? EventIconTypeIndex :
  T extends "EventItem" ? EventItemIndex :
  T extends "EventItemCastTimeline" ? EventItemCastTimelineIndex :
  T extends "EventItemHelp" ? EventItemHelpIndex :
  T extends "EventItemTimeline" ? EventItemTimelineIndex :
  T extends "EventSystemDefine" ? EventSystemDefineIndex :
  T extends "ExportedSG" ? ExportedSGIndex :
  T extends "ExVersion" ? ExVersionIndex :
  T extends "Fate" ? FateIndex :
  T extends "FateEvent" ? FateEventIndex :
  T extends "FateMode" ? FateModeIndex :
  T extends "FateProgressUI" ? FateProgressUIIndex :
  T extends "FateTokenType" ? FateTokenTypeIndex :
  T extends "FCActivity" ? FCActivityIndex :
  T extends "FCActivityCategory" ? FCActivityCategoryIndex :
  T extends "FCAuthority" ? FCAuthorityIndex :
  T extends "FCAuthorityCategory" ? FCAuthorityCategoryIndex :
  T extends "FCChestName" ? FCChestNameIndex :
  T extends "FCCrestSymbol" ? FCCrestSymbolIndex :
  T extends "FccShop" ? FccShopIndex :
  T extends "FCHierarchy" ? FCHierarchyIndex :
  T extends "FCProfile" ? FCProfileIndex :
  T extends "FCRank" ? FCRankIndex :
  T extends "FCReputation" ? FCReputationIndex :
  T extends "FCRights" ? FCRightsIndex :
  T extends "Festival" ? FestivalIndex :
  T extends "FieldMarker" ? FieldMarkerIndex :
  T extends "FishingRecordType" ? FishingRecordTypeIndex :
  T extends "FishingRecordTypeTransient" ? FishingRecordTypeTransientIndex :
  T extends "FishingSpot" ? FishingSpotIndex :
  T extends "FishParameter" ? FishParameterIndex :
  T extends "Frontline03" ? Frontline03Index :
  T extends "Frontline04" ? Frontline04Index :
  T extends "FurnitureCatalogCategory" ? FurnitureCatalogCategoryIndex :
  T extends "FurnitureCatalogItemList" ? FurnitureCatalogItemListIndex :
  T extends "GardeningSeed" ? GardeningSeedIndex :
  T extends "GatheringCondition" ? GatheringConditionIndex :
  T extends "GatheringExp" ? GatheringExpIndex :
  T extends "GatheringItem" ? GatheringItemIndex :
  T extends "GatheringItemLevelConvertTable" ? GatheringItemLevelConvertTableIndex :
  T extends "GatheringItemPoint" ? GatheringItemPointIndex :
  T extends "GatheringLeve" ? GatheringLeveIndex :
  T extends "GatheringLeveRoute" ? GatheringLeveRouteIndex :
  T extends "GatheringLeveRule" ? GatheringLeveRuleIndex :
  T extends "GatheringNotebookList" ? GatheringNotebookListIndex :
  T extends "GatheringPoint" ? GatheringPointIndex :
  T extends "GatheringPointBase" ? GatheringPointBaseIndex :
  T extends "GatheringPointBonus" ? GatheringPointBonusIndex :
  T extends "GatheringPointBonusType" ? GatheringPointBonusTypeIndex :
  T extends "GatheringPointName" ? GatheringPointNameIndex :
  T extends "GatheringPointTransient" ? GatheringPointTransientIndex :
  T extends "GatheringRarePopTimeTable" ? GatheringRarePopTimeTableIndex :
  T extends "GatheringSubCategory" ? GatheringSubCategoryIndex :
  T extends "GatheringType" ? GatheringTypeIndex :
  T extends "GcArmyCaptureTactics" ? GcArmyCaptureTacticsIndex :
  T extends "GcArmyExpedition" ? GcArmyExpeditionIndex :
  T extends "GcArmyExpeditionMemberBonus" ? GcArmyExpeditionMemberBonusIndex :
  T extends "GcArmyExpeditionType" ? GcArmyExpeditionTypeIndex :
  T extends "GcArmyMemberGrow" ? GcArmyMemberGrowIndex :
  T extends "GcArmyTraining" ? GcArmyTrainingIndex :
  T extends "GCRankGridaniaFemaleText" ? GCRankGridaniaFemaleTextIndex :
  T extends "GCRankGridaniaMaleText" ? GCRankGridaniaMaleTextIndex :
  T extends "GCRankLimsaFemaleText" ? GCRankLimsaFemaleTextIndex :
  T extends "GCRankLimsaMaleText" ? GCRankLimsaMaleTextIndex :
  T extends "GCRankUldahFemaleText" ? GCRankUldahFemaleTextIndex :
  T extends "GCRankUldahMaleText" ? GCRankUldahMaleTextIndex :
  T extends "GCScripShopCategory" ? GCScripShopCategoryIndex :
  T extends "GCScripShopItem" ? GCScripShopItemIndex :
  T extends "GCShop" ? GCShopIndex :
  T extends "GCShopItemCategory" ? GCShopItemCategoryIndex :
  T extends "GCSupplyDuty" ? GCSupplyDutyIndex :
  T extends "GCSupplyDutyReward" ? GCSupplyDutyRewardIndex :
  T extends "GeneralAction" ? GeneralActionIndex :
  T extends "GFATE" ? GFATEIndex :
  T extends "GFateClimbing2" ? GFateClimbing2Index :
  T extends "GFateClimbing2Content" ? GFateClimbing2ContentIndex :
  T extends "GFateClimbing2TotemType" ? GFateClimbing2TotemTypeIndex :
  T extends "GFateRideShooting" ? GFateRideShootingIndex :
  T extends "GilShop" ? GilShopIndex :
  T extends "GilShopItem" ? GilShopItemIndex :
  T extends "GimmickAccessor" ? GimmickAccessorIndex :
  T extends "GimmickJump" ? GimmickJumpIndex :
  T extends "GimmickRect" ? GimmickRectIndex :
  T extends "GoldSaucerArcadeMachine" ? GoldSaucerArcadeMachineIndex :
  T extends "GoldSaucerTextData" ? GoldSaucerTextDataIndex :
  T extends "GrandCompany" ? GrandCompanyIndex :
  T extends "GrandCompanyRank" ? GrandCompanyRankIndex :
  T extends "GroupPoseFrame" ? GroupPoseFrameIndex :
  T extends "GroupPoseStamp" ? GroupPoseStampIndex :
  T extends "GroupPoseStampCategory" ? GroupPoseStampCategoryIndex :
  T extends "GuardianDeity" ? GuardianDeityIndex :
  T extends "Guide" ? GuideIndex :
  T extends "GuidePage" ? GuidePageIndex :
  T extends "GuidePageString" ? GuidePageStringIndex :
  T extends "GuideTitle" ? GuideTitleIndex :
  T extends "GuildleveAssignment" ? GuildleveAssignmentIndex :
  T extends "GuildleveAssignmentCategory" ? GuildleveAssignmentCategoryIndex :
  T extends "GuildOrder" ? GuildOrderIndex :
  T extends "GuildOrderGuide" ? GuildOrderGuideIndex :
  T extends "GuildOrderOfficer" ? GuildOrderOfficerIndex :
  T extends "HairMakeType" ? HairMakeTypeIndex :
  T extends "HouseRetainerPose" ? HouseRetainerPoseIndex :
  T extends "HousingAethernet" ? HousingAethernetIndex :
  T extends "HousingAppeal" ? HousingAppealIndex :
  T extends "HousingEmploymentNpcList" ? HousingEmploymentNpcListIndex :
  T extends "HousingEmploymentNpcRace" ? HousingEmploymentNpcRaceIndex :
  T extends "HousingExterior" ? HousingExteriorIndex :
  T extends "HousingFurniture" ? HousingFurnitureIndex :
  T extends "HousingLandSet" ? HousingLandSetIndex :
  T extends "HousingMapMarkerInfo" ? HousingMapMarkerInfoIndex :
  T extends "HousingMerchantPose" ? HousingMerchantPoseIndex :
  T extends "HousingPlacement" ? HousingPlacementIndex :
  T extends "HousingPreset" ? HousingPresetIndex :
  T extends "HousingUnitedExterior" ? HousingUnitedExteriorIndex :
  T extends "HousingYardObject" ? HousingYardObjectIndex :
  T extends "HowTo" ? HowToIndex :
  T extends "HowToCategory" ? HowToCategoryIndex :
  T extends "HowToPage" ? HowToPageIndex :
  T extends "HugeCraftworksNpc" ? HugeCraftworksNpcIndex :
  T extends "HugeCraftworksRank" ? HugeCraftworksRankIndex :
  T extends "HWDAnnounce" ? HWDAnnounceIndex :
  T extends "HWDCrafterSupply" ? HWDCrafterSupplyIndex :
  T extends "HWDCrafterSupplyReward" ? HWDCrafterSupplyRewardIndex :
  T extends "HWDDevLayerControl" ? HWDDevLayerControlIndex :
  T extends "HWDDevLevelUI" ? HWDDevLevelUIIndex :
  T extends "HWDDevLively" ? HWDDevLivelyIndex :
  T extends "HWDDevProgress" ? HWDDevProgressIndex :
  T extends "HWDGathereInspectTerm" ? HWDGathereInspectTermIndex :
  T extends "HWDGathererInspection" ? HWDGathererInspectionIndex :
  T extends "HWDGathererInspectionReward" ? HWDGathererInspectionRewardIndex :
  T extends "HWDInfoBoardArticle" ? HWDInfoBoardArticleIndex :
  T extends "HWDInfoBoardArticleTransient" ? HWDInfoBoardArticleTransientIndex :
  T extends "HWDInfoBoardArticleType" ? HWDInfoBoardArticleTypeIndex :
  T extends "HWDIntroduction" ? HWDIntroductionIndex :
  T extends "HWDLevelChangeDeception" ? HWDLevelChangeDeceptionIndex :
  T extends "HWDSharedGroup" ? HWDSharedGroupIndex :
  T extends "HWDSharedGroupControlParam" ? HWDSharedGroupControlParamIndex :
  T extends "IKDContentBonus" ? IKDContentBonusIndex :
  T extends "IKDFishParam" ? IKDFishParamIndex :
  T extends "IKDRoute" ? IKDRouteIndex :
  T extends "IKDRouteTable" ? IKDRouteTableIndex :
  T extends "IKDSpot" ? IKDSpotIndex :
  T extends "InclusionShop" ? InclusionShopIndex :
  T extends "InclusionShopCategory" ? InclusionShopCategoryIndex :
  T extends "InclusionShopSeries" ? InclusionShopSeriesIndex :
  T extends "IndividualWeather" ? IndividualWeatherIndex :
  T extends "InstanceContent" ? InstanceContentIndex :
  T extends "InstanceContentBuff" ? InstanceContentBuffIndex :
  T extends "InstanceContentCSBonus" ? InstanceContentCSBonusIndex :
  T extends "InstanceContentGuide" ? InstanceContentGuideIndex :
  T extends "InstanceContentTextData" ? InstanceContentTextDataIndex :
  T extends "Item" ? ItemIndex :
  T extends "ItemAction" ? ItemActionIndex :
  T extends "ItemActionTelepo" ? ItemActionTelepoIndex :
  T extends "ItemBarterCheck" ? ItemBarterCheckIndex :
  T extends "ItemFood" ? ItemFoodIndex :
  T extends "ItemLevel" ? ItemLevelIndex :
  T extends "ItemSearchCategory" ? ItemSearchCategoryIndex :
  T extends "ItemSeries" ? ItemSeriesIndex :
  T extends "ItemSortCategory" ? ItemSortCategoryIndex :
  T extends "ItemSpecialBonus" ? ItemSpecialBonusIndex :
  T extends "ItemUICategory" ? ItemUICategoryIndex :
  T extends "JobHudManual" ? JobHudManualIndex :
  T extends "JobHudManualPriority" ? JobHudManualPriorityIndex :
  T extends "JournalCategory" ? JournalCategoryIndex :
  T extends "JournalGenre" ? JournalGenreIndex :
  T extends "JournalSection" ? JournalSectionIndex :
  T extends "Knockback" ? KnockbackIndex :
  T extends "LegacyQuest" ? LegacyQuestIndex :
  T extends "Leve" ? LeveIndex :
  T extends "LeveAssignmentType" ? LeveAssignmentTypeIndex :
  T extends "LeveClient" ? LeveClientIndex :
  T extends "Level" ? LevelIndex :
  T extends "LeveRewardItem" ? LeveRewardItemIndex :
  T extends "LeveRewardItemGroup" ? LeveRewardItemGroupIndex :
  T extends "LeveString" ? LeveStringIndex :
  T extends "LeveVfx" ? LeveVfxIndex :
  T extends "Lobby" ? LobbyIndex :
  T extends "LogFilter" ? LogFilterIndex :
  T extends "LogKind" ? LogKindIndex :
  T extends "LogKindCategoryText" ? LogKindCategoryTextIndex :
  T extends "LogMessage" ? LogMessageIndex :
  T extends "LotteryExchangeShop" ? LotteryExchangeShopIndex :
  T extends "MacroIcon" ? MacroIconIndex :
  T extends "MacroIconRedirectOld" ? MacroIconRedirectOldIndex :
  T extends "MainCommand" ? MainCommandIndex :
  T extends "MainCommandCategory" ? MainCommandCategoryIndex :
  T extends "ManeuversArmor" ? ManeuversArmorIndex :
  T extends "Map" ? MapIndex :
  T extends "MapMarker" ? MapMarkerIndex :
  T extends "MapMarkerRegion" ? MapMarkerRegionIndex :
  T extends "MapSymbol" ? MapSymbolIndex :
  T extends "Marker" ? MarkerIndex :
  T extends "MasterpieceSupplyDuty" ? MasterpieceSupplyDutyIndex :
  T extends "MasterpieceSupplyMultiplier" ? MasterpieceSupplyMultiplierIndex :
  T extends "Materia" ? MateriaIndex :
  T extends "MateriaJoinRate" ? MateriaJoinRateIndex :
  T extends "MateriaJoinRateGatherCraft" ? MateriaJoinRateGatherCraftIndex :
  T extends "MateriaTomestoneRate" ? MateriaTomestoneRateIndex :
  T extends "MiniGameRA" ? MiniGameRAIndex :
  T extends "MinionRace" ? MinionRaceIndex :
  T extends "MinionRules" ? MinionRulesIndex :
  T extends "MinionSkillType" ? MinionSkillTypeIndex :
  T extends "MobHuntOrder" ? MobHuntOrderIndex :
  T extends "MobHuntOrderType" ? MobHuntOrderTypeIndex :
  T extends "MobHuntReward" ? MobHuntRewardIndex :
  T extends "MobHuntRewardCap" ? MobHuntRewardCapIndex :
  T extends "MobHuntTarget" ? MobHuntTargetIndex :
  T extends "ModelChara" ? ModelCharaIndex :
  T extends "ModelSkeleton" ? ModelSkeletonIndex :
  T extends "ModelState" ? ModelStateIndex :
  T extends "MonsterNote" ? MonsterNoteIndex :
  T extends "MonsterNoteTarget" ? MonsterNoteTargetIndex :
  T extends "MotionTimeline" ? MotionTimelineIndex :
  T extends "MotionTimelineBlendTable" ? MotionTimelineBlendTableIndex :
  T extends "Mount" ? MountIndex :
  T extends "MountAction" ? MountActionIndex :
  T extends "MountCustomize" ? MountCustomizeIndex :
  T extends "MountFlyingCondition" ? MountFlyingConditionIndex :
  T extends "MountSpeed" ? MountSpeedIndex :
  T extends "MountTransient" ? MountTransientIndex :
  T extends "MoveTimeline" ? MoveTimelineIndex :
  T extends "MoveVfx" ? MoveVfxIndex :
  T extends "MovieSubtitle" ? MovieSubtitleIndex :
  T extends "MovieSubtitle500" ? MovieSubtitle500Index :
  T extends "MovieSubtitleVoyage" ? MovieSubtitleVoyageIndex :
  T extends "NotebookDivision" ? NotebookDivisionIndex :
  T extends "NotebookDivisionCategory" ? NotebookDivisionCategoryIndex :
  T extends "NotoriousMonster" ? NotoriousMonsterIndex :
  T extends "NpcEquip" ? NpcEquipIndex :
  T extends "NpcYell" ? NpcYellIndex :
  T extends "Omen" ? OmenIndex :
  T extends "OnlineStatus" ? OnlineStatusIndex :
  T extends "OpenContent" ? OpenContentIndex :
  T extends "OpenContentCandidateName" ? OpenContentCandidateNameIndex :
  T extends "Opening" ? OpeningIndex :
  T extends "Orchestrion" ? OrchestrionIndex :
  T extends "OrchestrionCategory" ? OrchestrionCategoryIndex :
  T extends "OrchestrionPath" ? OrchestrionPathIndex :
  T extends "OrchestrionUiparam" ? OrchestrionUiparamIndex :
  T extends "ParamGrow" ? ParamGrowIndex :
  T extends "PartyContent" ? PartyContentIndex :
  T extends "PartyContentCutscene" ? PartyContentCutsceneIndex :
  T extends "PartyContentTextData" ? PartyContentTextDataIndex :
  T extends "PatchMark" ? PatchMarkIndex :
  T extends "Perform" ? PerformIndex :
  T extends "PerformTransient" ? PerformTransientIndex :
  T extends "Pet" ? PetIndex :
  T extends "PetAction" ? PetActionIndex :
  T extends "PhysicsGroup" ? PhysicsGroupIndex :
  T extends "PhysicsWind" ? PhysicsWindIndex :
  T extends "Picture" ? PictureIndex :
  T extends "PlaceName" ? PlaceNameIndex :
  T extends "PlantPotFlowerSeed" ? PlantPotFlowerSeedIndex :
  T extends "PreHandler" ? PreHandlerIndex :
  T extends "PresetCamera" ? PresetCameraIndex :
  T extends "PresetCameraAdjust" ? PresetCameraAdjustIndex :
  T extends "PublicContent" ? PublicContentIndex :
  T extends "PublicContentCutscene" ? PublicContentCutsceneIndex :
  T extends "PublicContentTextData" ? PublicContentTextDataIndex :
  T extends "Purify" ? PurifyIndex :
  T extends "PvPAction" ? PvPActionIndex :
  T extends "PvPActionSort" ? PvPActionSortIndex :
  T extends "PvPRank" ? PvPRankIndex :
  T extends "PvPSelectTrait" ? PvPSelectTraitIndex :
  T extends "PvPTrait" ? PvPTraitIndex :
  T extends "Quest" ? QuestIndex :
  T extends "QuestBattle" ? QuestBattleIndex :
  T extends "QuestChapter" ? QuestChapterIndex :
  T extends "QuestClassJobReward" ? QuestClassJobRewardIndex :
  T extends "QuestClassJobSupply" ? QuestClassJobSupplyIndex :
  T extends "QuestDerivedClass" ? QuestDerivedClassIndex :
  T extends "QuestRedo" ? QuestRedoIndex :
  T extends "QuestRedoChapterUI" ? QuestRedoChapterUIIndex :
  T extends "QuestRedoChapterUICategory" ? QuestRedoChapterUICategoryIndex :
  T extends "QuestRedoChapterUITab" ? QuestRedoChapterUITabIndex :
  T extends "QuestRedoGroup" ? QuestRedoGroupIndex :
  T extends "QuestRedoIncompChapter" ? QuestRedoIncompChapterIndex :
  T extends "QuestRepeatFlag" ? QuestRepeatFlagIndex :
  T extends "QuestRewardOther" ? QuestRewardOtherIndex :
  T extends "QuickChat" ? QuickChatIndex :
  T extends "QuickChatTransient" ? QuickChatTransientIndex :
  T extends "Race" ? RaceIndex :
  T extends "RacingChocoboItem" ? RacingChocoboItemIndex :
  T extends "RacingChocoboName" ? RacingChocoboNameIndex :
  T extends "RacingChocoboNameCategory" ? RacingChocoboNameCategoryIndex :
  T extends "RacingChocoboNameInfo" ? RacingChocoboNameInfoIndex :
  T extends "RacingChocoboParam" ? RacingChocoboParamIndex :
  T extends "RecastNavimesh" ? RecastNavimeshIndex :
  T extends "Recipe" ? RecipeIndex :
  T extends "RecipeLevelTable" ? RecipeLevelTableIndex :
  T extends "RecipeLookup" ? RecipeLookupIndex :
  T extends "RecipeNotebookList" ? RecipeNotebookListIndex :
  T extends "RecommendContents" ? RecommendContentsIndex :
  T extends "Relic" ? RelicIndex :
  T extends "Relic3" ? Relic3Index :
  T extends "RelicItem" ? RelicItemIndex :
  T extends "RelicNote" ? RelicNoteIndex :
  T extends "RelicNoteCategory" ? RelicNoteCategoryIndex :
  T extends "Resident" ? ResidentIndex :
  T extends "RetainerTask" ? RetainerTaskIndex :
  T extends "RetainerTaskLvRange" ? RetainerTaskLvRangeIndex :
  T extends "RetainerTaskNormal" ? RetainerTaskNormalIndex :
  T extends "RetainerTaskParameter" ? RetainerTaskParameterIndex :
  T extends "RetainerTaskRandom" ? RetainerTaskRandomIndex :
  T extends "RideShooting" ? RideShootingIndex :
  T extends "RideShootingTextData" ? RideShootingTextDataIndex :
  T extends "RPParameter" ? RPParameterIndex :
  T extends "Salvage" ? SalvageIndex :
  T extends "SatisfactionArbitration" ? SatisfactionArbitrationIndex :
  T extends "SatisfactionNpc" ? SatisfactionNpcIndex :
  T extends "SatisfactionSupply" ? SatisfactionSupplyIndex :
  T extends "SatisfactionSupplyReward" ? SatisfactionSupplyRewardIndex :
  T extends "ScenarioTree" ? ScenarioTreeIndex :
  T extends "ScenarioTreeTips" ? ScenarioTreeTipsIndex :
  T extends "ScenarioTreeTipsClassQuest" ? ScenarioTreeTipsClassQuestIndex :
  T extends "ScenarioTreeTipsQuest" ? ScenarioTreeTipsQuestIndex :
  T extends "ScenarioType" ? ScenarioTypeIndex :
  T extends "ScreenImage" ? ScreenImageIndex :
  T extends "SecretRecipeBook" ? SecretRecipeBookIndex :
  T extends "SkyIsland2Mission" ? SkyIsland2MissionIndex :
  T extends "SkyIsland2MissionDetail" ? SkyIsland2MissionDetailIndex :
  T extends "SkyIsland2MissionType" ? SkyIsland2MissionTypeIndex :
  T extends "SkyIsland2RangeType" ? SkyIsland2RangeTypeIndex :
  T extends "SnipeTalk" ? SnipeTalkIndex :
  T extends "SnipeTalkName" ? SnipeTalkNameIndex :
  T extends "SpearfishingItem" ? SpearfishingItemIndex :
  T extends "SpearfishingNotebook" ? SpearfishingNotebookIndex :
  T extends "SpearfishingRecordPage" ? SpearfishingRecordPageIndex :
  T extends "SpecialShop" ? SpecialShopIndex :
  T extends "SpecialShopItemCategory" ? SpecialShopItemCategoryIndex :
  T extends "Stain" ? StainIndex :
  T extends "StainTransient" ? StainTransientIndex :
  T extends "Status" ? StatusIndex :
  T extends "StatusHitEffect" ? StatusHitEffectIndex :
  T extends "StatusLoopVFX" ? StatusLoopVFXIndex :
  T extends "Story" ? StoryIndex :
  T extends "SubmarineExploration" ? SubmarineExplorationIndex :
  T extends "SubmarineMap" ? SubmarineMapIndex :
  T extends "SubmarinePart" ? SubmarinePartIndex :
  T extends "SubmarineRank" ? SubmarineRankIndex :
  T extends "SwitchTalk" ? SwitchTalkIndex :
  T extends "SwitchTalkVariation" ? SwitchTalkVariationIndex :
  T extends "TerritoryType" ? TerritoryTypeIndex :
  T extends "TerritoryTypeTransient" ? TerritoryTypeTransientIndex :
  T extends "TextCommand" ? TextCommandIndex :
  T extends "Title" ? TitleIndex :
  T extends "Tomestones" ? TomestonesIndex :
  T extends "TomestonesItem" ? TomestonesItemIndex :
  T extends "TopicSelect" ? TopicSelectIndex :
  T extends "Town" ? TownIndex :
  T extends "Trait" ? TraitIndex :
  T extends "TraitRecast" ? TraitRecastIndex :
  T extends "TraitTransient" ? TraitTransientIndex :
  T extends "Transformation" ? TransformationIndex :
  T extends "Treasure" ? TreasureIndex :
  T extends "TreasureHuntRank" ? TreasureHuntRankIndex :
  T extends "TreasureModel" ? TreasureModelIndex :
  T extends "TreasureSpot" ? TreasureSpotIndex :
  T extends "Tribe" ? TribeIndex :
  T extends "TripleTriad" ? TripleTriadIndex :
  T extends "TripleTriadCard" ? TripleTriadCardIndex :
  T extends "TripleTriadCardRarity" ? TripleTriadCardRarityIndex :
  T extends "TripleTriadCardResident" ? TripleTriadCardResidentIndex :
  T extends "TripleTriadCardType" ? TripleTriadCardTypeIndex :
  T extends "TripleTriadCompetition" ? TripleTriadCompetitionIndex :
  T extends "TripleTriadRule" ? TripleTriadRuleIndex :
  T extends "Tutorial" ? TutorialIndex :
  T extends "TutorialDPS" ? TutorialDPSIndex :
  T extends "TutorialHealer" ? TutorialHealerIndex :
  T extends "TutorialTank" ? TutorialTankIndex :
  T extends "UIColor" ? UIColorIndex :
  T extends "VaseFlower" ? VaseFlowerIndex :
  T extends "VFX" ? VFXIndex :
  T extends "Warp" ? WarpIndex :
  T extends "WarpCondition" ? WarpConditionIndex :
  T extends "WarpLogic" ? WarpLogicIndex :
  T extends "WeaponTimeline" ? WeaponTimelineIndex :
  T extends "Weather" ? WeatherIndex :
  T extends "WeatherGroup" ? WeatherGroupIndex :
  T extends "WeatherRate" ? WeatherRateIndex :
  T extends "WeatherReportReplace" ? WeatherReportReplaceIndex :
  T extends "WeddingBGM" ? WeddingBGMIndex :
  T extends "WeeklyBingoOrderData" ? WeeklyBingoOrderDataIndex :
  T extends "WeeklyBingoRewardData" ? WeeklyBingoRewardDataIndex :
  T extends "WeeklyBingoText" ? WeeklyBingoTextIndex :
  T extends "WeeklyLotBonus" ? WeeklyLotBonusIndex :
  T extends "World" ? WorldIndex :
  T extends "WorldDCGroupType" ? WorldDCGroupTypeIndex :
  T extends "YardCatalogCategory" ? YardCatalogCategoryIndex :
  T extends "YardCatalogItemList" ? YardCatalogItemListIndex :
  T extends "YKW" ? YKWIndex :
  T extends "ZoneSharedGroup" ? ZoneSharedGroupIndex :
  void;


export type ApiResultFor<T> =
  T extends "Achievement" ? Achievement :
  T extends "AchievementCategory" ? AchievementCategory :
  T extends "AchievementHideCondition" ? AchievementHideCondition :
  T extends "AchievementKind" ? AchievementKind :
  T extends "Action" ? Action :
  T extends "ActionCastTimeline" ? ActionCastTimeline :
  T extends "ActionCastVFX" ? ActionCastVFX :
  T extends "ActionCategory" ? ActionCategory :
  T extends "ActionComboRoute" ? ActionComboRoute :
  T extends "ActionIndirection" ? ActionIndirection :
  T extends "ActionParam" ? ActionParam :
  T extends "ActionProcStatus" ? ActionProcStatus :
  T extends "ActionTimeline" ? ActionTimeline :
  T extends "ActionTimelineMove" ? ActionTimelineMove :
  T extends "ActionTimelineReplace" ? ActionTimelineReplace :
  T extends "ActionTransient" ? ActionTransient :
  T extends "ActivityFeedButtons" ? ActivityFeedButtons :
  T extends "ActivityFeedCaptions" ? ActivityFeedCaptions :
  T extends "ActivityFeedGroupCaptions" ? ActivityFeedGroupCaptions :
  T extends "ActivityFeedImages" ? ActivityFeedImages :
  T extends "Addon" ? Addon :
  T extends "AddonHud" ? AddonHud :
  T extends "Adventure" ? Adventure :
  T extends "AdventureExPhase" ? AdventureExPhase :
  T extends "AetherCurrent" ? AetherCurrent :
  T extends "AetherCurrentCompFlgSet" ? AetherCurrentCompFlgSet :
  T extends "AetherialWheel" ? AetherialWheel :
  T extends "Aetheryte" ? Aetheryte :
  T extends "AetheryteSystemDefine" ? AetheryteSystemDefine :
  T extends "AirshipExplorationLevel" ? AirshipExplorationLevel :
  T extends "AirshipExplorationLog" ? AirshipExplorationLog :
  T extends "AirshipExplorationParamType" ? AirshipExplorationParamType :
  T extends "AirshipExplorationPart" ? AirshipExplorationPart :
  T extends "AirshipExplorationPoint" ? AirshipExplorationPoint :
  T extends "AnimationLOD" ? AnimationLOD :
  T extends "AnimaWeapon5" ? AnimaWeapon5 :
  T extends "AnimaWeapon5Param" ? AnimaWeapon5Param :
  T extends "AnimaWeapon5PatternGroup" ? AnimaWeapon5PatternGroup :
  T extends "AnimaWeapon5SpiritTalk" ? AnimaWeapon5SpiritTalk :
  T extends "AnimaWeapon5SpiritTalkParam" ? AnimaWeapon5SpiritTalkParam :
  T extends "AnimaWeapon5TradeItem" ? AnimaWeapon5TradeItem :
  T extends "AnimaWeaponFUITalk" ? AnimaWeaponFUITalk :
  T extends "AnimaWeaponFUITalkParam" ? AnimaWeaponFUITalkParam :
  T extends "AnimaWeaponIcon" ? AnimaWeaponIcon :
  T extends "AozAction" ? AozAction :
  T extends "AozActionTransient" ? AozActionTransient :
  T extends "AOZArrangement" ? AOZArrangement :
  T extends "AOZBoss" ? AOZBoss :
  T extends "AOZContent" ? AOZContent :
  T extends "AOZContentBriefingBNpc" ? AOZContentBriefingBNpc :
  T extends "AquariumFish" ? AquariumFish :
  T extends "AquariumWater" ? AquariumWater :
  T extends "ArrayEventHandler" ? ArrayEventHandler :
  T extends "AttackType" ? AttackType :
  T extends "BacklightColor" ? BacklightColor :
  T extends "Ballista" ? Ballista :
  T extends "Balloon" ? Balloon :
  T extends "BaseParam" ? BaseParam :
  T extends "BattleLeve" ? BattleLeve :
  T extends "BattleLeveRule" ? BattleLeveRule :
  T extends "BeastRankBonus" ? BeastRankBonus :
  T extends "BeastReputationRank" ? BeastReputationRank :
  T extends "BeastTribe" ? BeastTribe :
  T extends "Behavior" ? Behavior :
  T extends "BehaviorPath" ? BehaviorPath :
  T extends "BenchmarkOverrideEquipment" ? BenchmarkOverrideEquipment :
  T extends "BGM" ? BGM :
  T extends "BGMFade" ? BGMFade :
  T extends "BGMFadeType" ? BGMFadeType :
  T extends "BGMScene" ? BGMScene :
  T extends "BGMSituation" ? BGMSituation :
  T extends "BGMSwitch" ? BGMSwitch :
  T extends "BGMSystemDefine" ? BGMSystemDefine :
  T extends "BNpcAnnounceIcon" ? BNpcAnnounceIcon :
  T extends "BNpcBase" ? BNpcBase :
  T extends "BNpcCustomize" ? BNpcCustomize :
  T extends "BNpcName" ? BNpcName :
  T extends "BNpcParts" ? BNpcParts :
  T extends "BNpcState" ? BNpcState :
  T extends "Buddy" ? Buddy :
  T extends "BuddyAction" ? BuddyAction :
  T extends "BuddyEquip" ? BuddyEquip :
  T extends "BuddyItem" ? BuddyItem :
  T extends "BuddyRank" ? BuddyRank :
  T extends "BuddySkill" ? BuddySkill :
  T extends "Cabinet" ? Cabinet :
  T extends "CabinetCategory" ? CabinetCategory :
  T extends "Calendar" ? Calendar :
  T extends "Carry" ? Carry :
  T extends "Channeling" ? Channeling :
  T extends "CharaMakeClassEquip" ? CharaMakeClassEquip :
  T extends "CharaMakeCustomize" ? CharaMakeCustomize :
  T extends "CharaMakeName" ? CharaMakeName :
  T extends "CharaMakeType" ? CharaMakeType :
  T extends "ChocoboRace" ? ChocoboRace :
  T extends "ChocoboRaceAbility" ? ChocoboRaceAbility :
  T extends "ChocoboRaceAbilityType" ? ChocoboRaceAbilityType :
  T extends "ChocoboRaceItem" ? ChocoboRaceItem :
  T extends "ChocoboRaceRank" ? ChocoboRaceRank :
  T extends "ChocoboRaceStatus" ? ChocoboRaceStatus :
  T extends "ChocoboRaceTerritory" ? ChocoboRaceTerritory :
  T extends "ChocoboRaceTutorial" ? ChocoboRaceTutorial :
  T extends "ChocoboRaceWeather" ? ChocoboRaceWeather :
  T extends "ChocoboTaxi" ? ChocoboTaxi :
  T extends "ChocoboTaxiStand" ? ChocoboTaxiStand :
  T extends "CircleActivity" ? CircleActivity :
  T extends "ClassJob" ? ClassJob :
  T extends "ClassJobCategory" ? ClassJobCategory :
  T extends "CollectablesShop" ? CollectablesShop :
  T extends "CollectablesShopItem" ? CollectablesShopItem :
  T extends "CollectablesShopItemGroup" ? CollectablesShopItemGroup :
  T extends "CollectablesShopRefine" ? CollectablesShopRefine :
  T extends "CollectablesShopRewardItem" ? CollectablesShopRewardItem :
  T extends "CollectablesShopRewardScrip" ? CollectablesShopRewardScrip :
  T extends "Companion" ? Companion :
  T extends "CompanionMove" ? CompanionMove :
  T extends "CompanionTransient" ? CompanionTransient :
  T extends "CompanyAction" ? CompanyAction :
  T extends "CompanyCraftDraft" ? CompanyCraftDraft :
  T extends "CompanyCraftDraftCategory" ? CompanyCraftDraftCategory :
  T extends "CompanyCraftManufactoryState" ? CompanyCraftManufactoryState :
  T extends "CompanyCraftPart" ? CompanyCraftPart :
  T extends "CompanyCraftProcess" ? CompanyCraftProcess :
  T extends "CompanyCraftSequence" ? CompanyCraftSequence :
  T extends "CompanyCraftSupplyItem" ? CompanyCraftSupplyItem :
  T extends "CompanyCraftType" ? CompanyCraftType :
  T extends "CompanyLeve" ? CompanyLeve :
  T extends "CompanyLeveRule" ? CompanyLeveRule :
  T extends "CompleteJournal" ? CompleteJournal :
  T extends "CompleteJournalCategory" ? CompleteJournalCategory :
  T extends "Condition" ? Condition :
  T extends "ConfigKey" ? ConfigKey :
  T extends "ContentCloseCycle" ? ContentCloseCycle :
  T extends "ContentExAction" ? ContentExAction :
  T extends "ContentFinderCondition" ? ContentFinderCondition :
  T extends "ContentFinderConditionTransient" ? ContentFinderConditionTransient :
  T extends "ContentGauge" ? ContentGauge :
  T extends "ContentGaugeColor" ? ContentGaugeColor :
  T extends "ContentMemberType" ? ContentMemberType :
  T extends "ContentNpcTalk" ? ContentNpcTalk :
  T extends "ContentRandomSelect" ? ContentRandomSelect :
  T extends "ContentRoulette" ? ContentRoulette :
  T extends "ContentRouletteOpenRule" ? ContentRouletteOpenRule :
  T extends "ContentRouletteRoleBonus" ? ContentRouletteRoleBonus :
  T extends "ContentsNote" ? ContentsNote :
  T extends "ContentTalk" ? ContentTalk :
  T extends "ContentTalkParam" ? ContentTalkParam :
  T extends "ContentType" ? ContentType :
  T extends "CraftAction" ? CraftAction :
  T extends "CraftLeve" ? CraftLeve :
  T extends "CraftLevelDifference" ? CraftLevelDifference :
  T extends "CraftType" ? CraftType :
  T extends "Credit" ? Credit :
  T extends "CreditCast" ? CreditCast :
  T extends "CreditListText" ? CreditListText :
  T extends "Currency" ? Currency :
  T extends "CustomTalk" ? CustomTalk :
  T extends "CustomTalkDynamicIcon" ? CustomTalkDynamicIcon :
  T extends "CustomTalkNestHandlers" ? CustomTalkNestHandlers :
  T extends "Cutscene" ? Cutscene :
  T extends "CutsceneMotion" ? CutsceneMotion :
  T extends "CutsceneWorkIndex" ? CutsceneWorkIndex :
  T extends "CutScreenImage" ? CutScreenImage :
  T extends "CycleTime" ? CycleTime :
  T extends "DailySupplyItem" ? DailySupplyItem :
  T extends "DawnContent" ? DawnContent :
  T extends "DawnGrowMember" ? DawnGrowMember :
  T extends "DawnMemberUIParam" ? DawnMemberUIParam :
  T extends "DawnQuestAnnounce" ? DawnQuestAnnounce :
  T extends "DawnQuestMember" ? DawnQuestMember :
  T extends "DeepDungeon" ? DeepDungeon :
  T extends "DeepDungeonBan" ? DeepDungeonBan :
  T extends "DeepDungeonDanger" ? DeepDungeonDanger :
  T extends "DeepDungeonEquipment" ? DeepDungeonEquipment :
  T extends "DeepDungeonFloorEffectUI" ? DeepDungeonFloorEffectUI :
  T extends "DeepDungeonItem" ? DeepDungeonItem :
  T extends "DeepDungeonLayer" ? DeepDungeonLayer :
  T extends "DeepDungeonMagicStone" ? DeepDungeonMagicStone :
  T extends "DeepDungeonMap5X" ? DeepDungeonMap5X :
  T extends "DeepDungeonRoom" ? DeepDungeonRoom :
  T extends "DeepDungeonStatus" ? DeepDungeonStatus :
  T extends "DefaultTalk" ? DefaultTalk :
  T extends "DefaultTalkLipSyncType" ? DefaultTalkLipSyncType :
  T extends "DeliveryQuest" ? DeliveryQuest :
  T extends "Description" ? Description :
  T extends "DescriptionPage" ? DescriptionPage :
  T extends "DescriptionSection" ? DescriptionSection :
  T extends "DescriptionString" ? DescriptionString :
  T extends "DisposalShop" ? DisposalShop :
  T extends "DisposalShopFilterType" ? DisposalShopFilterType :
  T extends "DisposalShopItem" ? DisposalShopItem :
  T extends "DpsChallenge" ? DpsChallenge :
  T extends "DpsChallengeOfficer" ? DpsChallengeOfficer :
  T extends "DpsChallengeTransient" ? DpsChallengeTransient :
  T extends "EmjAddon" ? EmjAddon :
  T extends "EmjDani" ? EmjDani :
  T extends "Emote" ? Emote :
  T extends "EmoteCategory" ? EmoteCategory :
  T extends "EmoteMode" ? EmoteMode :
  T extends "ENpcBase" ? ENpcBase :
  T extends "ENpcDressUp" ? ENpcDressUp :
  T extends "ENpcDressUpDress" ? ENpcDressUpDress :
  T extends "ENpcResident" ? ENpcResident :
  T extends "EObj" ? EObj :
  T extends "EObjName" ? EObjName :
  T extends "EquipRaceCategory" ? EquipRaceCategory :
  T extends "EquipSlotCategory" ? EquipSlotCategory :
  T extends "EurekaAetherItem" ? EurekaAetherItem :
  T extends "EurekaAethernet" ? EurekaAethernet :
  T extends "EurekaGrowData" ? EurekaGrowData :
  T extends "EurekaLogosMixerProbability" ? EurekaLogosMixerProbability :
  T extends "EurekaMagiaAction" ? EurekaMagiaAction :
  T extends "EurekaMagiciteItem" ? EurekaMagiciteItem :
  T extends "EurekaMagiciteItemType" ? EurekaMagiciteItemType :
  T extends "EurekaSphereElementAdjust" ? EurekaSphereElementAdjust :
  T extends "EventAction" ? EventAction :
  T extends "EventIconPriority" ? EventIconPriority :
  T extends "EventIconType" ? EventIconType :
  T extends "EventItem" ? EventItem :
  T extends "EventItemCastTimeline" ? EventItemCastTimeline :
  T extends "EventItemHelp" ? EventItemHelp :
  T extends "EventItemTimeline" ? EventItemTimeline :
  T extends "EventSystemDefine" ? EventSystemDefine :
  T extends "ExportedSG" ? ExportedSG :
  T extends "ExVersion" ? ExVersion :
  T extends "Fate" ? Fate :
  T extends "FateEvent" ? FateEvent :
  T extends "FateMode" ? FateMode :
  T extends "FateProgressUI" ? FateProgressUI :
  T extends "FateTokenType" ? FateTokenType :
  T extends "FCActivity" ? FCActivity :
  T extends "FCActivityCategory" ? FCActivityCategory :
  T extends "FCAuthority" ? FCAuthority :
  T extends "FCAuthorityCategory" ? FCAuthorityCategory :
  T extends "FCChestName" ? FCChestName :
  T extends "FCCrestSymbol" ? FCCrestSymbol :
  T extends "FccShop" ? FccShop :
  T extends "FCHierarchy" ? FCHierarchy :
  T extends "FCProfile" ? FCProfile :
  T extends "FCRank" ? FCRank :
  T extends "FCReputation" ? FCReputation :
  T extends "FCRights" ? FCRights :
  T extends "Festival" ? Festival :
  T extends "FieldMarker" ? FieldMarker :
  T extends "FishingRecordType" ? FishingRecordType :
  T extends "FishingRecordTypeTransient" ? FishingRecordTypeTransient :
  T extends "FishingSpot" ? FishingSpot :
  T extends "FishParameter" ? FishParameter :
  T extends "Frontline03" ? Frontline03 :
  T extends "Frontline04" ? Frontline04 :
  T extends "FurnitureCatalogCategory" ? FurnitureCatalogCategory :
  T extends "FurnitureCatalogItemList" ? FurnitureCatalogItemList :
  T extends "GardeningSeed" ? GardeningSeed :
  T extends "GatheringCondition" ? GatheringCondition :
  T extends "GatheringExp" ? GatheringExp :
  T extends "GatheringItem" ? GatheringItem :
  T extends "GatheringItemLevelConvertTable" ? GatheringItemLevelConvertTable :
  T extends "GatheringItemPoint" ? GatheringItemPoint :
  T extends "GatheringLeve" ? GatheringLeve :
  T extends "GatheringLeveRoute" ? GatheringLeveRoute :
  T extends "GatheringLeveRule" ? GatheringLeveRule :
  T extends "GatheringNotebookList" ? GatheringNotebookList :
  T extends "GatheringPoint" ? GatheringPoint :
  T extends "GatheringPointBase" ? GatheringPointBase :
  T extends "GatheringPointBonus" ? GatheringPointBonus :
  T extends "GatheringPointBonusType" ? GatheringPointBonusType :
  T extends "GatheringPointName" ? GatheringPointName :
  T extends "GatheringPointTransient" ? GatheringPointTransient :
  T extends "GatheringRarePopTimeTable" ? GatheringRarePopTimeTable :
  T extends "GatheringSubCategory" ? GatheringSubCategory :
  T extends "GatheringType" ? GatheringType :
  T extends "GcArmyCaptureTactics" ? GcArmyCaptureTactics :
  T extends "GcArmyExpedition" ? GcArmyExpedition :
  T extends "GcArmyExpeditionMemberBonus" ? GcArmyExpeditionMemberBonus :
  T extends "GcArmyExpeditionType" ? GcArmyExpeditionType :
  T extends "GcArmyMemberGrow" ? GcArmyMemberGrow :
  T extends "GcArmyTraining" ? GcArmyTraining :
  T extends "GCRankGridaniaFemaleText" ? GCRankGridaniaFemaleText :
  T extends "GCRankGridaniaMaleText" ? GCRankGridaniaMaleText :
  T extends "GCRankLimsaFemaleText" ? GCRankLimsaFemaleText :
  T extends "GCRankLimsaMaleText" ? GCRankLimsaMaleText :
  T extends "GCRankUldahFemaleText" ? GCRankUldahFemaleText :
  T extends "GCRankUldahMaleText" ? GCRankUldahMaleText :
  T extends "GCScripShopCategory" ? GCScripShopCategory :
  T extends "GCScripShopItem" ? GCScripShopItem :
  T extends "GCShop" ? GCShop :
  T extends "GCShopItemCategory" ? GCShopItemCategory :
  T extends "GCSupplyDuty" ? GCSupplyDuty :
  T extends "GCSupplyDutyReward" ? GCSupplyDutyReward :
  T extends "GeneralAction" ? GeneralAction :
  T extends "GFATE" ? GFATE :
  T extends "GFateClimbing2" ? GFateClimbing2 :
  T extends "GFateClimbing2Content" ? GFateClimbing2Content :
  T extends "GFateClimbing2TotemType" ? GFateClimbing2TotemType :
  T extends "GFateRideShooting" ? GFateRideShooting :
  T extends "GilShop" ? GilShop :
  T extends "GilShopItem" ? GilShopItem :
  T extends "GimmickAccessor" ? GimmickAccessor :
  T extends "GimmickJump" ? GimmickJump :
  T extends "GimmickRect" ? GimmickRect :
  T extends "GoldSaucerArcadeMachine" ? GoldSaucerArcadeMachine :
  T extends "GoldSaucerTextData" ? GoldSaucerTextData :
  T extends "GrandCompany" ? GrandCompany :
  T extends "GrandCompanyRank" ? GrandCompanyRank :
  T extends "GroupPoseFrame" ? GroupPoseFrame :
  T extends "GroupPoseStamp" ? GroupPoseStamp :
  T extends "GroupPoseStampCategory" ? GroupPoseStampCategory :
  T extends "GuardianDeity" ? GuardianDeity :
  T extends "Guide" ? Guide :
  T extends "GuidePage" ? GuidePage :
  T extends "GuidePageString" ? GuidePageString :
  T extends "GuideTitle" ? GuideTitle :
  T extends "GuildleveAssignment" ? GuildleveAssignment :
  T extends "GuildleveAssignmentCategory" ? GuildleveAssignmentCategory :
  T extends "GuildOrder" ? GuildOrder :
  T extends "GuildOrderGuide" ? GuildOrderGuide :
  T extends "GuildOrderOfficer" ? GuildOrderOfficer :
  T extends "HairMakeType" ? HairMakeType :
  T extends "HouseRetainerPose" ? HouseRetainerPose :
  T extends "HousingAethernet" ? HousingAethernet :
  T extends "HousingAppeal" ? HousingAppeal :
  T extends "HousingEmploymentNpcList" ? HousingEmploymentNpcList :
  T extends "HousingEmploymentNpcRace" ? HousingEmploymentNpcRace :
  T extends "HousingExterior" ? HousingExterior :
  T extends "HousingFurniture" ? HousingFurniture :
  T extends "HousingLandSet" ? HousingLandSet :
  T extends "HousingMapMarkerInfo" ? HousingMapMarkerInfo :
  T extends "HousingMerchantPose" ? HousingMerchantPose :
  T extends "HousingPlacement" ? HousingPlacement :
  T extends "HousingPreset" ? HousingPreset :
  T extends "HousingUnitedExterior" ? HousingUnitedExterior :
  T extends "HousingYardObject" ? HousingYardObject :
  T extends "HowTo" ? HowTo :
  T extends "HowToCategory" ? HowToCategory :
  T extends "HowToPage" ? HowToPage :
  T extends "HugeCraftworksNpc" ? HugeCraftworksNpc :
  T extends "HugeCraftworksRank" ? HugeCraftworksRank :
  T extends "HWDAnnounce" ? HWDAnnounce :
  T extends "HWDCrafterSupply" ? HWDCrafterSupply :
  T extends "HWDCrafterSupplyReward" ? HWDCrafterSupplyReward :
  T extends "HWDDevLayerControl" ? HWDDevLayerControl :
  T extends "HWDDevLevelUI" ? HWDDevLevelUI :
  T extends "HWDDevLively" ? HWDDevLively :
  T extends "HWDDevProgress" ? HWDDevProgress :
  T extends "HWDGathereInspectTerm" ? HWDGathereInspectTerm :
  T extends "HWDGathererInspection" ? HWDGathererInspection :
  T extends "HWDGathererInspectionReward" ? HWDGathererInspectionReward :
  T extends "HWDInfoBoardArticle" ? HWDInfoBoardArticle :
  T extends "HWDInfoBoardArticleTransient" ? HWDInfoBoardArticleTransient :
  T extends "HWDInfoBoardArticleType" ? HWDInfoBoardArticleType :
  T extends "HWDIntroduction" ? HWDIntroduction :
  T extends "HWDLevelChangeDeception" ? HWDLevelChangeDeception :
  T extends "HWDSharedGroup" ? HWDSharedGroup :
  T extends "HWDSharedGroupControlParam" ? HWDSharedGroupControlParam :
  T extends "IKDContentBonus" ? IKDContentBonus :
  T extends "IKDFishParam" ? IKDFishParam :
  T extends "IKDRoute" ? IKDRoute :
  T extends "IKDRouteTable" ? IKDRouteTable :
  T extends "IKDSpot" ? IKDSpot :
  T extends "InclusionShop" ? InclusionShop :
  T extends "InclusionShopCategory" ? InclusionShopCategory :
  T extends "InclusionShopSeries" ? InclusionShopSeries :
  T extends "IndividualWeather" ? IndividualWeather :
  T extends "InstanceContent" ? InstanceContent :
  T extends "InstanceContentBuff" ? InstanceContentBuff :
  T extends "InstanceContentCSBonus" ? InstanceContentCSBonus :
  T extends "InstanceContentGuide" ? InstanceContentGuide :
  T extends "InstanceContentTextData" ? InstanceContentTextData :
  T extends "Item" ? Item :
  T extends "ItemAction" ? ItemAction :
  T extends "ItemActionTelepo" ? ItemActionTelepo :
  T extends "ItemFood" ? ItemFood :
  T extends "ItemLevel" ? ItemLevel :
  T extends "ItemSearchCategory" ? ItemSearchCategory :
  T extends "ItemSeries" ? ItemSeries :
  T extends "ItemSortCategory" ? ItemSortCategory :
  T extends "ItemSpecialBonus" ? ItemSpecialBonus :
  T extends "ItemUICategory" ? ItemUICategory :
  T extends "JobHudManual" ? JobHudManual :
  T extends "JobHudManualPriority" ? JobHudManualPriority :
  T extends "JournalCategory" ? JournalCategory :
  T extends "JournalGenre" ? JournalGenre :
  T extends "JournalSection" ? JournalSection :
  T extends "Knockback" ? Knockback :
  T extends "LegacyQuest" ? LegacyQuest :
  T extends "Leve" ? Leve :
  T extends "LeveAssignmentType" ? LeveAssignmentType :
  T extends "LeveClient" ? LeveClient :
  T extends "Level" ? Level :
  T extends "LeveRewardItem" ? LeveRewardItem :
  T extends "LeveRewardItemGroup" ? LeveRewardItemGroup :
  T extends "LeveString" ? LeveString :
  T extends "LeveVfx" ? LeveVfx :
  T extends "Lobby" ? Lobby :
  T extends "LogFilter" ? LogFilter :
  T extends "LogKind" ? LogKind :
  T extends "LogKindCategoryText" ? LogKindCategoryText :
  T extends "LogMessage" ? LogMessage :
  T extends "LotteryExchangeShop" ? LotteryExchangeShop :
  T extends "MacroIcon" ? MacroIcon :
  T extends "MacroIconRedirectOld" ? MacroIconRedirectOld :
  T extends "MainCommand" ? MainCommand :
  T extends "MainCommandCategory" ? MainCommandCategory :
  T extends "ManeuversArmor" ? ManeuversArmor :
  T extends "Map" ? Map :
  T extends "MapMarker" ? MapMarker :
  T extends "MapMarkerRegion" ? MapMarkerRegion :
  T extends "MapSymbol" ? MapSymbol :
  T extends "Marker" ? Marker :
  T extends "MasterpieceSupplyDuty" ? MasterpieceSupplyDuty :
  T extends "MasterpieceSupplyMultiplier" ? MasterpieceSupplyMultiplier :
  T extends "Materia" ? Materia :
  T extends "MateriaJoinRate" ? MateriaJoinRate :
  T extends "MateriaJoinRateGatherCraft" ? MateriaJoinRateGatherCraft :
  T extends "MateriaTomestoneRate" ? MateriaTomestoneRate :
  T extends "MiniGameRA" ? MiniGameRA :
  T extends "MinionRace" ? MinionRace :
  T extends "MinionRules" ? MinionRules :
  T extends "MinionSkillType" ? MinionSkillType :
  T extends "MobHuntOrder" ? MobHuntOrder :
  T extends "MobHuntOrderType" ? MobHuntOrderType :
  T extends "MobHuntReward" ? MobHuntReward :
  T extends "MobHuntRewardCap" ? MobHuntRewardCap :
  T extends "MobHuntTarget" ? MobHuntTarget :
  T extends "ModelChara" ? ModelChara :
  T extends "ModelSkeleton" ? ModelSkeleton :
  T extends "ModelState" ? ModelState :
  T extends "MonsterNote" ? MonsterNote :
  T extends "MonsterNoteTarget" ? MonsterNoteTarget :
  T extends "MotionTimeline" ? MotionTimeline :
  T extends "MotionTimelineBlendTable" ? MotionTimelineBlendTable :
  T extends "Mount" ? Mount :
  T extends "MountAction" ? MountAction :
  T extends "MountCustomize" ? MountCustomize :
  T extends "MountFlyingCondition" ? MountFlyingCondition :
  T extends "MountSpeed" ? MountSpeed :
  T extends "MountTransient" ? MountTransient :
  T extends "MoveTimeline" ? MoveTimeline :
  T extends "MoveVfx" ? MoveVfx :
  T extends "MovieSubtitle" ? MovieSubtitle :
  T extends "MovieSubtitle500" ? MovieSubtitle500 :
  T extends "MovieSubtitleVoyage" ? MovieSubtitleVoyage :
  T extends "NotebookDivision" ? NotebookDivision :
  T extends "NotebookDivisionCategory" ? NotebookDivisionCategory :
  T extends "NotoriousMonster" ? NotoriousMonster :
  T extends "NpcEquip" ? NpcEquip :
  T extends "NpcYell" ? NpcYell :
  T extends "Omen" ? Omen :
  T extends "OnlineStatus" ? OnlineStatus :
  T extends "OpenContent" ? OpenContent :
  T extends "OpenContentCandidateName" ? OpenContentCandidateName :
  T extends "Opening" ? Opening :
  T extends "Orchestrion" ? Orchestrion :
  T extends "OrchestrionCategory" ? OrchestrionCategory :
  T extends "OrchestrionPath" ? OrchestrionPath :
  T extends "OrchestrionUiparam" ? OrchestrionUiparam :
  T extends "ParamGrow" ? ParamGrow :
  T extends "PartyContent" ? PartyContent :
  T extends "PartyContentCutscene" ? PartyContentCutscene :
  T extends "PartyContentTextData" ? PartyContentTextData :
  T extends "PatchMark" ? PatchMark :
  T extends "Perform" ? Perform :
  T extends "PerformTransient" ? PerformTransient :
  T extends "Pet" ? Pet :
  T extends "PetAction" ? PetAction :
  T extends "PhysicsGroup" ? PhysicsGroup :
  T extends "PhysicsWind" ? PhysicsWind :
  T extends "Picture" ? Picture :
  T extends "PlaceName" ? PlaceName :
  T extends "PlantPotFlowerSeed" ? PlantPotFlowerSeed :
  T extends "PreHandler" ? PreHandler :
  T extends "PresetCamera" ? PresetCamera :
  T extends "PresetCameraAdjust" ? PresetCameraAdjust :
  T extends "PublicContent" ? PublicContent :
  T extends "PublicContentCutscene" ? PublicContentCutscene :
  T extends "PublicContentTextData" ? PublicContentTextData :
  T extends "Purify" ? Purify :
  T extends "PvPAction" ? PvPAction :
  T extends "PvPActionSort" ? PvPActionSort :
  T extends "PvPRank" ? PvPRank :
  T extends "PvPSelectTrait" ? PvPSelectTrait :
  T extends "PvPTrait" ? PvPTrait :
  T extends "Quest" ? Quest :
  T extends "QuestBattle" ? QuestBattle :
  T extends "QuestChapter" ? QuestChapter :
  T extends "QuestClassJobReward" ? QuestClassJobReward :
  T extends "QuestClassJobSupply" ? QuestClassJobSupply :
  T extends "QuestDerivedClass" ? QuestDerivedClass :
  T extends "QuestRedo" ? QuestRedo :
  T extends "QuestRedoChapterUI" ? QuestRedoChapterUI :
  T extends "QuestRedoChapterUICategory" ? QuestRedoChapterUICategory :
  T extends "QuestRedoChapterUITab" ? QuestRedoChapterUITab :
  T extends "QuestRedoGroup" ? QuestRedoGroup :
  T extends "QuestRedoIncompChapter" ? QuestRedoIncompChapter :
  T extends "QuestRepeatFlag" ? QuestRepeatFlag :
  T extends "QuestRewardOther" ? QuestRewardOther :
  T extends "QuickChat" ? QuickChat :
  T extends "QuickChatTransient" ? QuickChatTransient :
  T extends "Race" ? Race :
  T extends "RacingChocoboItem" ? RacingChocoboItem :
  T extends "RacingChocoboName" ? RacingChocoboName :
  T extends "RacingChocoboNameCategory" ? RacingChocoboNameCategory :
  T extends "RacingChocoboNameInfo" ? RacingChocoboNameInfo :
  T extends "RacingChocoboParam" ? RacingChocoboParam :
  T extends "RecastNavimesh" ? RecastNavimesh :
  T extends "Recipe" ? Recipe :
  T extends "RecipeLevelTable" ? RecipeLevelTable :
  T extends "RecipeLookup" ? RecipeLookup :
  T extends "RecipeNotebookList" ? RecipeNotebookList :
  T extends "RecommendContents" ? RecommendContents :
  T extends "Relic" ? Relic :
  T extends "Relic3" ? Relic3 :
  T extends "RelicItem" ? RelicItem :
  T extends "RelicNote" ? RelicNote :
  T extends "RelicNoteCategory" ? RelicNoteCategory :
  T extends "Resident" ? Resident :
  T extends "RetainerTask" ? RetainerTask :
  T extends "RetainerTaskLvRange" ? RetainerTaskLvRange :
  T extends "RetainerTaskNormal" ? RetainerTaskNormal :
  T extends "RetainerTaskParameter" ? RetainerTaskParameter :
  T extends "RetainerTaskRandom" ? RetainerTaskRandom :
  T extends "RideShooting" ? RideShooting :
  T extends "RideShootingTextData" ? RideShootingTextData :
  T extends "RPParameter" ? RPParameter :
  T extends "Salvage" ? Salvage :
  T extends "SatisfactionArbitration" ? SatisfactionArbitration :
  T extends "SatisfactionNpc" ? SatisfactionNpc :
  T extends "SatisfactionSupply" ? SatisfactionSupply :
  T extends "SatisfactionSupplyReward" ? SatisfactionSupplyReward :
  T extends "ScenarioTree" ? ScenarioTree :
  T extends "ScenarioTreeTips" ? ScenarioTreeTips :
  T extends "ScenarioTreeTipsClassQuest" ? ScenarioTreeTipsClassQuest :
  T extends "ScenarioTreeTipsQuest" ? ScenarioTreeTipsQuest :
  T extends "ScenarioType" ? ScenarioType :
  T extends "ScreenImage" ? ScreenImage :
  T extends "SecretRecipeBook" ? SecretRecipeBook :
  T extends "SkyIsland2Mission" ? SkyIsland2Mission :
  T extends "SkyIsland2MissionDetail" ? SkyIsland2MissionDetail :
  T extends "SkyIsland2MissionType" ? SkyIsland2MissionType :
  T extends "SkyIsland2RangeType" ? SkyIsland2RangeType :
  T extends "SnipeTalk" ? SnipeTalk :
  T extends "SnipeTalkName" ? SnipeTalkName :
  T extends "SpearfishingItem" ? SpearfishingItem :
  T extends "SpearfishingNotebook" ? SpearfishingNotebook :
  T extends "SpearfishingRecordPage" ? SpearfishingRecordPage :
  T extends "SpecialShop" ? SpecialShop :
  T extends "SpecialShopItemCategory" ? SpecialShopItemCategory :
  T extends "Stain" ? Stain :
  T extends "StainTransient" ? StainTransient :
  T extends "Status" ? Status :
  T extends "StatusHitEffect" ? StatusHitEffect :
  T extends "StatusLoopVFX" ? StatusLoopVFX :
  T extends "Story" ? Story :
  T extends "SubmarineExploration" ? SubmarineExploration :
  T extends "SubmarineMap" ? SubmarineMap :
  T extends "SubmarinePart" ? SubmarinePart :
  T extends "SubmarineRank" ? SubmarineRank :
  T extends "SwitchTalk" ? SwitchTalk :
  T extends "SwitchTalkVariation" ? SwitchTalkVariation :
  T extends "TerritoryType" ? TerritoryType :
  T extends "TerritoryTypeTransient" ? TerritoryTypeTransient :
  T extends "TextCommand" ? TextCommand :
  T extends "Title" ? Title :
  T extends "Tomestones" ? Tomestones :
  T extends "TomestonesItem" ? TomestonesItem :
  T extends "TopicSelect" ? TopicSelect :
  T extends "Town" ? Town :
  T extends "Trait" ? Trait :
  T extends "TraitRecast" ? TraitRecast :
  T extends "TraitTransient" ? TraitTransient :
  T extends "Transformation" ? Transformation :
  T extends "Treasure" ? Treasure :
  T extends "TreasureHuntRank" ? TreasureHuntRank :
  T extends "TreasureModel" ? TreasureModel :
  T extends "TreasureSpot" ? TreasureSpot :
  T extends "Tribe" ? Tribe :
  T extends "TripleTriad" ? TripleTriad :
  T extends "TripleTriadCard" ? TripleTriadCard :
  T extends "TripleTriadCardRarity" ? TripleTriadCardRarity :
  T extends "TripleTriadCardResident" ? TripleTriadCardResident :
  T extends "TripleTriadCardType" ? TripleTriadCardType :
  T extends "TripleTriadCompetition" ? TripleTriadCompetition :
  T extends "TripleTriadRule" ? TripleTriadRule :
  T extends "Tutorial" ? Tutorial :
  T extends "TutorialDPS" ? TutorialDPS :
  T extends "TutorialHealer" ? TutorialHealer :
  T extends "TutorialTank" ? TutorialTank :
  T extends "UIColor" ? UIColor :
  T extends "VaseFlower" ? VaseFlower :
  T extends "VFX" ? VFX :
  T extends "Warp" ? Warp :
  T extends "WarpCondition" ? WarpCondition :
  T extends "WarpLogic" ? WarpLogic :
  T extends "WeaponTimeline" ? WeaponTimeline :
  T extends "Weather" ? Weather :
  T extends "WeatherGroup" ? WeatherGroup :
  T extends "WeatherRate" ? WeatherRate :
  T extends "WeatherReportReplace" ? WeatherReportReplace :
  T extends "WeddingBGM" ? WeddingBGM :
  T extends "WeeklyBingoOrderData" ? WeeklyBingoOrderData :
  T extends "WeeklyBingoRewardData" ? WeeklyBingoRewardData :
  T extends "WeeklyBingoText" ? WeeklyBingoText :
  T extends "WeeklyLotBonus" ? WeeklyLotBonus :
  T extends "World" ? World :
  T extends "WorldDCGroupType" ? WorldDCGroupType :
  T extends "YardCatalogCategory" ? YardCatalogCategory :
  T extends "YardCatalogItemList" ? YardCatalogItemList :
  T extends "YKW" ? YKW :
  T extends "ZoneSharedGroup" ? ZoneSharedGroup :
  void;